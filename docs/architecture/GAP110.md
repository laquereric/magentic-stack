# Gap 110: quarantine population is the in-scope census

`check_shape_quarantine` iterated `shape_owner_candidates.json` (171
rows) and commented that the graph may contain extras. After gap 107,
scope and binding report **174** NodeShapes; quarantine still printed
171 and `contextframe` was absent from the inventory.

An unowned shape that never enters the population is invisible. 0
examined is not a pass; a real number over a stale subset is the same
shape.

## What the census is now

Union of:

- in-scope NodeShapes (same sweep as `check_shape_scope`)
- binding-manifest `shapes[].local_name`
- NodeShapes in the loaded graph

A live name missing from the inventory is a FAIL. Empty CHECK_ROOT is
a FAIL.

`--write` refreshes the inventory from that census. ContextFrame /
Meaning / Clarification now appear (unowned, self_targeting: they have
`sh:targetClass` and no wrap).

Did not delete anything. Did not start the non-all-200 plan or the
catalog port.
