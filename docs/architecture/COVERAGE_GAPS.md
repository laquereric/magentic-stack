# Gap analysis

Diagrams and per-container detail: [`ContainerTopology.md`](ContainerTopology.md).

Baseline measured 2026-08-31 at `030af95`; rows re-measured since carry their
own date in-cell. Doctrine: ADR 0046 (vault separate), 0047 +
amendments (three languages, container boundaries, one Rails app many ROLEs),
0048 (MIND serves a CPCP seam), 0049 (ROLE=shape from mounted gems), 0050
(SwitchYard is the upstream; bus and persist are Rails ROLEs).

**Target: 14 running containers, 7 images.** One Rails application under
**eight running** ROLEs (project-graph stays embedded in BACK, row 7), plus
MIND (Python), SwitchYard, oxigraph, and **nats** (ADR 0065, in-pod L7 broker).

Row numbers are stable across revisions so they can be cited; the DB_PATH block
(39-47) was inserted later and therefore sits out of sequence.

## 0. Rollup

| State | Rows | Meaning |
|---|---|---|
| **prerequisite** | 117 | **one open**: no proven actor, so ADR 0070 cannot be satisfied (39, 43 closed as gated) |
| **owner decision** | — | **none open** (121 closed: count is 14; this file was unregistered in CLAIMS) |
| **next** | — | leftover, not these five. Groups 1–3 and 5 are work. Group 4 is the stop list. Group 6 is dormant / later. Group 7 is amendment and operations debt. Group 8 is owner acts and publication. Group 9 is honest exclusions. Group 10 is observation leftovers. Group 11 is an undeclared seam and recorded shape deltas. Group 12 is superseded closed-row measurements. Group 13 is unenforced flags whose because names a landing as unbuilt. Group 14 is an ungated human pin, and facts the pin landing recorded. Group 15 is not this tree, or filling it here would lie. Group 16 is facts the FRONT compose swap recorded. Group 17 is the seventh image lineage, and facts the count fix recorded. Group 18 is the double-landing, and facts the merge recorded. Group 19 is prose the swap dated. 117 is not next. 121 is closed. See §Next leftover coverage. |
| delegated | — | none |
| open | 10 | known (10: slices 2–5 built except CI wiring), unscheduled |
| rework pending | — | none |
| decided, unbuilt | 86, 87 | ruled; nothing built yet |
| reframed | 13, 23 | the row as written was the wrong question |
| carve-out or unowned | 25, 26, 27, 28, 29, 30, 118, 119, 120 | section 3 (outside the language rule); section 6 (survey-derived boundary gaps) |
| closed | 1, 2, 3, 7, 14, 16, 21, 24, 42, 44, 47, 51, 52, 53, 54, 55, 56, 57, 58, 60, 62, 63, 66, 67, 71, 74, 76, 77, 78, 79, 80, 64, 65, 88, 89, 90, 93, 92, 61, 96, 12, 95, 98, 100, 99, 97, 81, 101, 69, 59, 20, 103, 94, 68, 49, 50, 5, 108, 107, 110, 109, 111, 15, 112, 113, 46, 22, 4, 6, 104, 19, 17, 18, 114, 115, 39, 43, 8, 83, 84, 85, 11, 72, 73, 82, 91, 45, 9, 40, 70, 102, 48, 75, 41, 105, 106, 116, 121 | 97 rows |
| no state by design | 31-38 | sections 4 and 5 are descriptive tables with no State column |

_Rollup accounting re-run 2026-09-16 at `dd5b732`, path rewritten same day: **117 rows mentioned, 117 distinct, none in two lines, none missing.** Population: **117 rows**, of which 109 carry a State column and 8 (31-38) do not; every row appears in exactly one line above. State was read as the last cell, after confirming each section has a uniform field count, so no embedded pipe shifts which cell is read. The `## Critical path` table has its own numbering (now 1, 2, 3 — the original 1, 2, 2b, 3, 3b, 4, 5 path is complete) and is excluded._

_**Two corrections this re-run made, recorded rather than absorbed.** (1) **Row 116 was missing from the rollup.** It was added to section 1 when `nats` landed (ADR 0065) and never entered a state line, so the prior note's "none missing" was false for it, and its "Population: 115 rows" undercounted by one. 116 is `closed` and now appears on the closed line. Adding row 117 is what surfaced it, which is the argument for re-running the accounting on every insertion rather than trusting the last note. (2) The prior note named the `## Critical path` numbering as `1, 2, 2b, 3, 3b`; it is **`1, 2, 2b, 3, 3b, 4, 5`** — seven rows, not five. The exclusion was applied correctly either way (the section is excluded whole), but the parenthetical was wrong and would mislead anyone re-deriving the count by hand._

_**Rollup accounting re-run again 2026-09-16**, on the boundary-gaps reconciliation: **120 rows mentioned, 120 distinct, none in two lines, none missing.** Population: **120 rows**, of which 112 carry a State column and 8 (31-38) do not. Section 6 adds 118, 119 and 120, all `carve-out or unowned` -- survey-derived, so none of them is `open`. BoundaryGaps gap 5 deliberately gets **no row**: it is blocked on 117, and a blocked item carrying a number of its own reads as parallel work. The `## Critical path` table keeps its own numbering (now 1 — row 117 only) and stays excluded. `## Next leftover coverage` groups 1–16 are excluded the same way — 10 / 86 / 87 already live on `open` / `decided, unbuilt`; 29 / 30 already live on `carve-out or unowned`; 121 is `owner decision`; group 4 cites closed / reframed / carve-out rows; group 5 cites follow-ups on closed 6 and 109; group 6 cites dormant/later rows; group 7 cites amendment and operations debt on closed rows; group 8 cites owner acts and publication; group 9 cites honest exclusions on closed rows; group 10 cites observation leftovers that were explicitly not done; group 11 cites an undeclared seam and recorded shape deltas; group 12 cites superseded closed-row measurements; group 13 cites unenforced flags whose because names a landing as unbuilt; group 14 cites an ungated human pin and facts the pin landing recorded; group 15 cites work that is not this tree, or that would lie if filled here; group 16 cites facts the FRONT compose swap recorded; group 17 cites the seventh image lineage and facts the count fix recorded; group 18 cites the double-landing and facts the merge recorded; group 19 cites prose the swap dated._

_**Rollup accounting re-run 2026-09-16** on the container-count record: **121 rows mentioned, 121 distinct, none in two lines, none missing.** Population: **121 rows**, of which 113 carry a State column and 8 (31-38) do not. Row 121 is `owner decision` — the first entry on that line since it was emptied. It records a measured contradiction and does not resolve it: picking the number is the owner's. Group 5 Order 11 now points here instead of asking the leftover table to pick 12 or 14._

_**Rollup accounting re-run 2026-09-16** on the group-6 merge: **121 rows mentioned, 121 distinct, none in two lines, none missing.** Population: **121 rows**, 113 with a State column and 8 (31-38) without. Group 6 adds no rows: Orders 12-15 are citation numbers inside `## Next leftover coverage`, excluded whole like Critical path. The merge collided on two rollup lines and each was taken from the side that was right -- `owner decision` from row 121 (121, not 'none open'), `next` from group 6 (which names the dormant set). The earlier note still reads 120 and is left as written: it dates the boundary-gaps re-run, and this file records re-runs rather than absorbing them._

## 1. Containers

| # | Container | Image | Today | Gap | State |
|---:|---|---|---|---|---|
| 1 | `back` | Rails | `ROLE=back`, a declared domain writer (ADR 0056), draws `/_cpcp` | ~~shares `mind-data` with `backjob`~~ **that is now the correct arrangement** — two declared writers | closed |
| 2 | `backjob` | Rails | `ROLE=backjob`, CPCP completion works | ~~`Reconciliation.create!` is a violation~~ **CLOSED by ADR 0056 as a DECLARATION, not a fix.** BACKJOB is a declared co-writer. New work moves to rows 76-79 | closed |
| 3 | `front` | Rails | `ROLE=front`, route-gated off `/_cpcp` | served the write seam until `0a7d67f`; historical overlay gone — see row 24 | done |
| 4 | `vault` | Rails | **CLOSED.** `ROLE=vault` serves `POST /_cpcp/rpc` on `VaultCpcpController` (not stock `RpcController`). REST `/secrets` retired. Methods unchanged (put/list/get). Config-admin still put+list only, now JSON-RPC. Port unpublished. Gate: `check_vault_cpcp.py`. Findings: [`GAP4.md`](../archive/findings/GAP4.md) | — | closed |
| 5 | `config-admin` | Rails | **CLOSED.** `ROLE=config` on the shared image. Host `:13003`. Vault client put+list only, REST stand-in, `Net::HTTP#request` so a 403 keeps `reason`/`because` (gap 104). Fail-closed boot. Not `/_cpcp`, not get. Row 15 catalogue lives here (`GET /catalog`); discovery/verify stay on switch. Switch `:13001` stays until row 11. Did not migrate vault. Did not touch `.agent/secrets`. Gate: `check_config_vault_client.py`. Findings: [`GAP5.md`](../archive/findings/GAP5.md) | — | closed |
| 6 | `shape` | Rails | **CLOSED.** `ROLE=shape` v1 serves GET retrieval only: `/_cpcp/up`, `/_cpcp/shapes.json`, `/_cpcp/shapes/sha256:<digest>` on `ShapeController`. Catalog from `ProfileCatalog.default` grouped by file digest; TTL bytes served only when the re-hashed file matches the requested digest, else 404. Engine NOT mounted (would draw POST rpc and BACK's note.create). Unpublished, DBless, `domain_writers` shape writes []. Enforcement stays in BACK. **The row’s replacement reason was itself half wrong**: `shapes-level-8` IS in the map (13 rows) — only the P2–P8/P10 half held. Re-measured `incomplete_because` now also names `contextframe.shacl.ttl` and `profile-9-ghis.ttl` as on disk but absent from `ProfileCatalog`. Empty catalog is `ok:false` `shape_catalog_empty`. 0049 `enforced_by` gains the checker + spec, staying `unenforced: true` as an explicit **partial**. Gate: `check_role_shape.py` (9 examined); plants incl. `engine-mount-fails`, `empty-ok-true-fails`, `false-reason-fails`, `host-publish-fails` | — | closed |
| 7 | `project-graph` | Rails | **Mechanical half landed.** Storable IS wired (`project_on_save!`). Canonical `MM_OXIGRAPH_URL` was **two** env settings (back, backjob), not three — the third grep hit was the comment. Extract now sets the same two plus `VV_GRAPH_TRIPLE_GATE=strict`. SPARQL `{ok:false}` on emit is RefusalLog `graph_unreachable` with restoration, not `:applied`. Gate: `check_oxigraph_env.py` | **12th container spent on nats (ADR 0065, 2026-09-08).** Projection stays embedded in BACK (ROW7 option 1: measured working). Async `ROLE=project-graph` is no longer the slot. | closed |
| 8 | ~~`persist`~~ | Rails | **BUILT.** `ROLE=persist` serves `POST /_cpcp/rpc` (`persist.path.set/get`) on its own controller, own sqlite on `persist-data`, allowlisted callers, fail-closed boot. Placement intentions against the closed set, never live (`live_applied:false`). Findings: [`GAP8.md`](../archive/findings/GAP8.md). Gate: `check_role_persist.py` | row 41 unblocked; row 48 still decided-unbuilt | closed |
| 9 | `bus` | Rails | **RUNS** as of `0ed7db3`; **own sqlite** as of the bus-independent-sqlite plan (`BUS_DB_PATH` on `bus-data`, `BusProjection` on a second Rails DB, domain file mounted `:ro` as source) | seam + projection built (row 18). Decided: nothing CALLs `bus.projection.latest` on a schedule — async side-channel pulled on demand (row 72); GAP18 recorded the unbuilt caller rather than inventing one, and that stands | closed |
| 10 | `mind` | Python | CPCP **client** only: `CMD ["harness.py"]`, no `EXPOSE`, no inbound surface | **Slices 2–5 BUILT** — [`GAP10_SEAM.md`](../archive/findings/GAP10_SEAM.md): stdlib seam serves the 3 approved methods (admit-not-execute queue, retained readings), gated by `check_mind_seam.py`, live-proven. Per-cycle LLM Python cells retained + served ([`GAP10_CELLS.md`](../archive/findings/GAP10_CELLS.md)), durable in the NOOA store when bound. Seam authority live; BACK-pull gate, conformance (10/10 live), prompt re-audit done ([`GAP10_CONF.md`](../archive/findings/GAP10_CONF.md)). Remaining: CI wiring of the battery (snippet ready). | open, narrowed |
| 11 | `SwitchYard` | NVIDIA Rust + CPCP endpoint | **v1 LANDED** `37a355d`. **STEP 0 cleared: the pin builds and starts** (rustc 1.96.1) — it had never been executed here. Shape D: the adapter under `gems/adapters/nemo-switchyard/` wraps the pin (env injection, loopback :4000, algorithms noop/passthrough/random only); `server.mjs` forwards `/v1/*`; `router.mjs` is no longer the data-plane decision. Same compose service, 8789 unpublished, 8790 published, **no new ports**. Pin NOT moved, `upstreams/` untouched. Language rule still a Node **violation**. The content-blind ruling is **gated, not hoped**: plants `judge-fails`, `escalation-fails`, `routequery-fails`, `publish-fails`. Gate: `check_switchyard_algorithms.py` (12 examined) | **Slices A–D DONE** — [`GAP11_KEYS.md`](../archive/findings/GAP11_KEYS.md), [`GAP11_UI.md`](../archive/findings/GAP11_UI.md), [`GAP11_RETIRE.md`](../archive/findings/GAP11_RETIRE.md), [`GAP11_CACHE.md`](../archive/findings/GAP11_CACHE.md): keys from vault, display in config-admin, `:13001` retired, cache verdict (no consumer — dormant, tripwired). | closed |
| 12 | `graph` | oxigraph, third-party | running, digest-pinned | **CLOSED.** Exemption IS written (0047:89, 166) and is now a **condition** a container must meet (third-party, unforked, digest-pinned, no source), not a table cell. Gate: `check_language_rule.py`. `switch` is a named **violation** (row 11), not an exemption. Findings: [`GAP12.md`](../archive/findings/GAP12.md) | — | closed |
| 116 | `nats` | official nats | **RUNS (12th container, ADR 0065).** Digest-pinned `nats:2.11.16@sha256:8f09…`, unpublished `:4222`, JetStream on `nats-data`. Not ROLE=bus. In-pod CPCP is NATS-exclusive when `MM_NATS_URL` is set (HTTP is not a fallback). Gate: `check_nats.py` + `check_nats_exclusive.py` | removing in-pod `expose: 3000` is follow-up | closed |

## 2. Cross-cutting gaps

| # | Gap | Measured today | Blocks | State |
|---:|---|---|---|---|
| 13 | **CORRECTED.** The shapes ARE the language-neutral spec | `grounding.rb:123`: "The shapes are the specification and CI runs pyshacl over them with fixtures"; the Ruby is a deliberate hand-written reproduction because `mm-shacl-reader` is not wired in-process. `pyshacl` is already pinned | what is missing is **distribution**, not authorship -> row 6 | reframed |
| 14 | ~~Behavioural contract is Ruby-only~~ | **WRITTEN** at `55dbd82`: `ContainerTopology`'s companion `CPCP_BEHAVIOUR.md` — 324 lines, 63 `file:line` citations, **16 SPECIFIED vs 38 OBSERVED**, plus what could not be determined | — | closed |
| 15 | ~~SwitchYard parity: catalogue, discovery, verification~~ | **CLOSED.** Catalogue table is `runtimes/mind-pod/app/config/llm_catalog.json`, served `GET /catalog` on `ROLE=config` (display only). `catalog.mjs` remains as routing helpers and loads that JSON; switch image COPYs it. `discovery.mjs` and `verify.mjs` stay on switch (token holder singular). None dropped. Did not add `get`. Did not invent a config-to-switch probe RPC. Gate: `check_config_catalog.py`. Findings: [`GAP15.md`](../archive/findings/GAP15.md) | row 11 still carries discovery+verify as router charter | closed |
| 16 | ~~Bus vs persist~~ | **CLOSED.** BUS implements RES; PERSIST determines the filesystem write location for the Event Store. Departs from memo 0830d deliberately — that memo reasoned about a Rust router holding credentials; the premise moved | — | closed |
| 17 | ~~`rails_event_store` is a new dependency~~ | **DECLINED by the owner 2026-09-03**, on the recommendation of [`ROW17.md`](../archive/findings/ROW17.md) `5390919`. **The decisive finding is that RES would be a SECOND event log.** ADR 0052 says the operation journal is the *only* admission truth and row 73 keeps it in BACK; RES doctrine says the event store *is* the source of truth. Two append-only ordered logs in two ROLEs, and calling one “metadata” does not stop it being a log — adopting would have required amending 0052 or retracting 0050’s “BUS owns the event log’s content”. After rows 72/73 the leftover job is an async side-channel of derived metadata, which a table plus the existing journal already does. **A table plus the journal is the bus** | ADR 0050 now owes an amendment: BUS is the seam + projection, not an event store | closed |
| 18 | ~~The CPCP endpoint language, and do-not-fork~~ | **CLOSED** `0ed7db3`. `ROLE=bus` serves `POST /_cpcp/rpc` on its own `BusCpcpController` — engine NOT mounted (`note.create` is BACK’s; stock `RpcController` is all-200). One method, `bus.projection.latest`, retains counts derived from BACK’s journal into `bus_projections`. **No RES** (row 17 declined). Seam moved decided-unbuilt → **live**, live seams 3 → 4, and `authoritative_for` no longer says “RES metadata”. `domain_writers` gains a **third case**: named `writes` — neither `all_domain` nor `[]`. Row 72 is gated, not just asserted: `back-dials-bus-fails` proves BACK, backjob, home_controller and the initializer do not dial bus. Additive migration only (`create_table` + index). Gate: `check_role_bus.py` (13 examined) | — | closed |
| 19 | ~~Upstream is pre-alpha by its own README~~ | **CLOSED** `cd1166a` + `f3eb5eb`. ADR **0061** quotes upstream’s Maturity section verbatim and records that we sit **64 commits past `v0.2.0-rc2`, untagged** — sharper than “pre-alpha” alone. Three layers must agree: ADR `accepted_pin`, `pin.json pinned_revision`, and the gitlink; CI runs `submodules: true` so the worktree-drift arm actually executes. A move requires a `reviews[]` row (`sha/from_sha/at/by/looked_at/because`), and `empty-looked-at-fails` means a hollow record does not count. **0061 is gated for real**: `enforced_by` names the checker and the workflow, no `unenforced` flag. First cut regressed `adapters-sole-path-to-upstreams` (per-line rule: a line naming `upstreams/` must also name `upstreams/manifests`); fixed by reading `submodule_path` from `pin.json` instead of a second literal, **fail-closed if absent** — not by exempting `tooling/pins` | — | closed |
| 20 | ~~FIVE CPCP seams; authority stated for one~~ | **CLOSED.** Gap 79 already rewrote live prose. **Measured, not inherited:** live **2** (BACK rails-cpcp; switchyard-offline local). decided-unbuilt was 2; **gap 50 moved vault from not-a-seam to decided-unbuilt** (CPCP inbound decided, REST still the stand-in). 0050's four names are not the live count; this row's five is not either. Not-a-seam: FRONT, `runtimes/switch`. Table: `seam_authority.json`. Gate: `check_seam_authority.py`. Findings: [`GAP20.md`](../archive/findings/GAP20.md) | — | closed |
| 21 | ~~Route-gating not CI-gated~~ | **CLOSED.** `check_role_routes.py` boots each ROLE and diffs the DRAWN table against `role_routes.json`; roles discovered both-ways from `entrypoint.sh` + both composes. 4 boots, ~5s | — | closed, see 51 |
| 22 | ~~`osi.example` successors NAMED; the resolver is the remainder~~ | **CLOSED as resolver.** `l8.context.list` keeps stored `shape_id` and adds `shape_id_resolved` / `shape_id_resolution` (`historical` \| `current` \| `unresolved`). Unknown IRIs get `{ok:false, reason: shape_id_unresolved}` on the row, not a guessed successor and not a nil. Mapping file is the single source. No UPDATE. No TTL/`@prefix`/catalog rename. Gate: `check_shape_id_resolver.py`. Findings: [`GAP22.md`](../archive/findings/GAP22.md) | rename remains a later compatibility event | closed |
| 23 | ~~Shape payload part-migrated — "7 of 59"~~ | **THE FRACTION WAS WRONG AND I REPEATED IT.** 7/59 compares a runtime pin to a mixed tree. The 52 is: **26 pyshacl example fixtures (not shapes at all)**, 4 ontology, 2 allowlist templates, 20 under `shapes/` of which 5 are byte-identical P1/P2 copies — **unique shape contents ≈ 15, not 52**. And step 10 is RETIRE after soak, not move. There is almost nothing to MOVE | no useful single number exists; retirement, not migration | reframed |
| 24 | ~~FRONT historical exposure~~ | **CLOSED.** Reachable until `0a7d67f` (extract compose published FRONT `:13000`, routes mounted `/_cpcp` for every ROLE, image baked a writable sqlite). FRONT overlay `/rails/db/mind_pod.sqlite3` is gone with the containers. Image sqlite is the Aug 19 host snapshot, not a FRONT overlay. Volume `mind-pod-demo_mind-data` is BACK. Findings: [`GAP24_FRONT_CPCP.md`](../archive/findings/GAP24_FRONT_CPCP.md) | — | closed |
| 49 | ~~Vault refusals vs the never-raise envelope~~ | **CLOSED by gap 50.** KEEP BOTH is now in the contract: refusals are **non-200** AND `{ok:false, reason:, because:}`. Stock `RailsCpcp::RpcController` (always HTTP 200) must not serve vault refusals unmodified. Gate: `check_vault_cpcp.py` (a 200-on-refusal plant fails). Findings: [`GAP50.md`](../archive/findings/GAP50.md) | — | closed |
| 50 | ~~Contract-before-caller sequencing~~ | **CLOSED.** Contract decided: three methods (`vault.secret.put` / `list` / `get`), allowlist on the method ahead of the store, config-admin cannot get, Bearer header not a param, non-200 plus envelope, stock RpcController out, IRIs under `https://w3id.org/cpcp/osi8/vault#` (ADR 0060 convention), TTL not authored (row 6). Vault is decided-unbuilt, not live, not not-a-seam. REST remains the stand-in (row 4). Did not build config-admin. Gate: `check_vault_cpcp.py`. Findings: [`GAP50.md`](../archive/findings/GAP50.md) | — | closed |
| 51 | ~~Route gate excludes `/rails*` by path string~~ | **CLOSED** at `02e59a2`, and closed harder than briefed: the path-prefix skip is GONE, not made provenance-based, and any `skipped` entry is now itself a FAIL. My plant `GET /rails/backdoor` on `vault` -> `vault extra GET /rails/backdoor`, exit 1 | — | closed |
| 52 | ~~A P6 denial is stored as `admitted`~~ | **CLOSED** at `6803c72`. Column dropped; admission derived from the journal in three states + an `indeterminate` alarm. Verified in SQL on the real host DB: `cid:sha256:cb841fdf` journals `received,grounded,refused` while the column said `admitted` — column predicate selected 4, journal derivation selects 3 | — | closed |
| 53 | ~~Two replay body shapes for one write~~ | **CLOSED** at `53f98e2`. One replay document for any PUSH replay, compact (absent cids omitted); first success stays domain-shaped. Caller analysis covered all nine; none break | — | closed |
| 54 | ~~Invalid JSON becomes `unknown_operation`~~ | **CLOSED.** `empty_body` and `unparseable_json` are now distinct refusals, specified as != `unknown_operation` | — | closed |
| 55 | ~~Dispatcher retry reports `replayed:false`~~ | **CLOSED** with 53 — same defect, the frozen first response | — | closed |
| 56 | ~~`l8.execution.complete` rows have an empty journal~~ | **CLOSED** at `f1dab59` (ADR 0053). Complete rows journal themselves — `received` at creation, `completed` after the receipt. Additive: parent journals verified unchanged (7 entries before and after). Still `not_an_admission`; `NOT_AN_ADMISSION_NAMES` untouched; indeterminate still 0 | — | closed |
| 57 | ~~2 historical rows unclassifiable~~ | **CLOSED.** Backfilled `authorized` entries carrying `backfill:true`, `not_a_p6_permit:true`, the reason, ADR 0052 and a timestamp — a reader can always tell P6 never saw them | — | closed |
| 58 | ~~`indeterminate` must stay empty~~ | **CLOSED.** `check_admission_indeterminate.py` gates it; the migration itself refuses to drop the column when the count is non-zero, and writes the gate artifact BEFORE aborting | — | closed |
| 59 | ~~The CPCP seam already has Python and JS callers~~ | **CLOSED.** Census stands: 22 token sites; callers of BACK = 2 Python + 3 Ruby + 0 JS. Plant `new-python-caller-fails` was a harness bug: `SKIP_DIR` included `tmp`, and `Path.parts` on a `/tmp/gap59-*` CHECK_ROOT skipped the planted file, so the gate never saw it (`hit=False`) while a hand plant in the real tree worked. Skip is now relative to CHECK_ROOT. Plant table: clean / empty-root / drop-caller / new-python-caller all pass. Findings: [`GAP59.md`](../archive/findings/GAP59.md) | — | closed |
| 60 | ~~The app image bakes local dev databases~~ | **CLOSED.** `app/.dockerignore` excludes `db/*.sqlite3` and WAL/SHM. `check_no_baked_sqlite.py` gates the file and, when `MIND_POD_IMAGE` is set, `find`s the image. FRONT boots `/up` and `/` with no sqlite in the image (vault-test probe and post-dockerignore rebuild); `/governance` 500 in the probe was a view `nil[]` with BACK down, not AR. Did **not** add `db:prepare` to FRONT | — | closed |
| 61 | ~~`make demo` cannot run, and the docs teach a 404~~ | **CLOSED.** `make demo` uses the existing CI overlay and BACK on `:3000`. Extract publishes BACK on `:13002` so FRONT `:13000` stays the web page. `cid.json` `backUrl` is `:13002`. Gate: `check_host_cpcp.py` (`localhost:13000/_cpcp` in host-facing files is a FAIL). Findings: [`GAP61.md`](../archive/findings/GAP61.md) | — | closed |
| 62 | ~~MIND system prompt false about the graph~~ | **CLOSED** at `b1f871b`. Established from source that the CLASS docstring IS the system prompt (`agent.py:437`) and the METHOD docstring the task prompt (`prompts.py:64`); the module docstring is human-facing and was audited anyway. FALSE claims removed, not promised: SPARQL as MIND's surface, RDF knowledge today, named-graph layout, sole-writer singular, "bounded SELECT" | — | closed |
| 63 | ~~Never-raise has no observer~~ (ADR 0054) | **STEP 2.** One collector (`RefusalLog` JSONL + heartbeat), not 29 edits. Dispatcher observes `Envelope.fail` and nested `{ok:false}`. No-context sites (publisher, backjob, blob open, idempotency, FRONT) call `record` without changing their return values. Gate: `check_refusal_observer.py`. Findings: [`GAP63_STEP2.md`](../archive/findings/GAP63_STEP2.md) | 8 UNCLEAR remain (64, 65) | closed as collector; UNCLEAR not in scope |
| 68 | ~~5 of the 8 UNCLEAR need a CONTRACT change~~ | **CLOSED.** `U3` gap 64, `U6` gap 69, `U7` gap 65, **`U2` stays nil by design.** **`U5`:** `storable_models` returns `{ok:true, models:}` or `{ok:false, reason: :storable_discovery_failed}`; `run` forwards the discovery reason and keeps `:no_storable_models` for a successful empty catalogue. `U1`, `U4`, `U8` stay observation-only. Did not touch idempotency `get`. Did not run `graph.replay` on the live volume. Gate: `check_graph_replay_discovery.py`. Findings: [`GAP68.md`](../archive/findings/GAP68.md) | — | closed |
| 69 | ~~U6: projection proceeds WITHOUT durability~~ | **CLOSED.** `schema_status` is `:available` / `:missing` / `:check_failed`. Immediate refuses on the last two (`outbox_not_installed`, `outbox_schema_check_failed`) with a restoration object; it does **not** `create_table` at runtime and does **not** drain without an outbox. `ensure_schema!` remains for specs. Honest production install is the gem migration in the app — **done in gap 94**. Gate: `check_outbox_schema.py`. Plants: drop `schema_status`; plant lazy `ensure_schema!`; restore `connected?`. Findings: [`GAP69.md`](../archive/findings/GAP69.md) | — | closed |
| 70 | **No sentinel, no out-parameter, no Dry::Monads** | Manus rejects sentinels (missed by truthiness, equality, serialization, cross-language clients) and out-parameters (non-idiomatic, aliasing, does not solve durable observation). Recommends an OWNED plain-data envelope — hashes/booleans/strings/numbers/arrays/null only — so Python, JS and Rust can consume it | **satisfied by construction and gated**: all four seams speak owned plain-data envelopes, no monad deps, no sentinels — `check_no_sentinels.py` (62 files) | closed |
| 71 | ~~Two integrity authorities~~ | **CLOSED.** BACK and BACKJOB write domain state (ADR 0056); BUS is the source of truth for METADATA (ADR 0057). Neither is "integrity" in the abstract. RES records successes AND failures; BACK and MIND HANDLE them — recording and handling are different jobs with different owners | — | closed |
| 72 | ~~Does BUS become a runtime dependency of every call?~~ | **DECIDED 2026-09-02: NO -- BUS is asynchronous.** It retains and publishes metadata but is not required for a call to complete. `/_cpcp/rpc` stays a synchronous POST that BACK completes alone. Availability is not coupled to BUS | **BUILT** as decided (row 18 + `back-dials-bus` gate): BUS retains/projects, BACK never dials it | closed |
| 73 | ~~What state actually moves to BUS~~ | **DECIDED 2026-09-02: the OperationRequest row and its journal STAY IN BACK.** ADRs 0052 and 0053 corrected exactly those admission/journal invariants; moving the state would invalidate rulings already made. BUS retains metadata derived from them, it does not own them | **BUILT** as decided: journal + row stay in BACK (052/053 unamended), BUS projects `bus_projections` into its own file | closed |
| 74 | ~~Envelope refusals are not restoration-grade~~ | **CLOSED.** ONE new attribute key, `cpcp.restoration` = state_reached / inconsistency / restore_when / restore_action, from roughly sixteen proposed fields. Six mapped to OTEL-native, three omitted rather than fabricated, two dropped. All-or-nothing: any blank key drops the whole object, verified 7/7 independently | RestorationShape excluded from the OSI L8 census with a stated reason; 171 in-scope, 5 exclusions | closed |
| 75 | **BUS changes must force participant rebuilds** | ADR 0055 clause 4. **Owners named: BACK and MIND** handle build-time alignment. Crosses images and languages — MIND is Python, SwitchYard Rust | we have cid identity, SHACL as the neutral contract, and fail-closed gates; missing is which BUS changes are BREAKING, and a gate refusing a participant pinned to a superseded contract | **closed**: breaking classes defined on `Bus::Projector::CONTRACT_VERSION` (bump on removal/rename/narrowing; additive is free), version served on `latest`, code-to-spec pinned by `check_bus_contract.py`; participant-side refusal activates with the first versioned consumer | closed |
| 76 | ~~"Sole writers" does not say who writes what~~ | **CLOSED** at `db1de82`. `config/domain_writers.json` states it; `DomainWriters` gates it on `before_save`/`before_destroy` **plus** `sql.active_record`, because `insert_all` bypasses callbacks. `unknown_role: refuse` | — | closed |
| 77 | ~~WAL inherited, not declared~~ | **CLOSED.** `database.yml` now declares `pragmas.journal_mode: wal`. Measured before and after: already `wal` from Rails 8 `DEFAULT_PRAGMAS`, so the runtime is unchanged and the **guarantee** is what was delivered | — | closed |
| 78 | ~~SQLITE_BUSY would be invisible~~ | **CLOSED.** A real `BEGIN IMMEDIATE` collision recorded as `reason=sqlite_busy` in `RefusalLog` — its first real customer. Instrumentation, not a contract change: BACKJOB's loop has no caller | — | closed |
| 79 | ~~"BACK is the sole writer" false everywhere~~ | **CLOSED for LIVE PROSE.** routes.rb, both compose headers, READMEs, QUICKSTART, bootstrap, rails_cpcp.rb, engine.rb, translation_board.rb. ADRs deliberately untouched — a dated record gets an amendment, not an edit. The CPCP "sole writer on the far side" text is still TRUE: the seam far-side is BACK | — | closed |
| 80 | ~~"Durable memory" misclassifies MIND's store~~ | **CLOSED.** The prompt now says inference memory that is "nondeterministic inference state, not application state and not metadata" — the three-way division taught explicitly. "Durable" now attaches only to what BACK commits: *committing it makes it durable rather than true* | — | closed |
| 81 | ~~A fourth audit verdict: MISCLASSIFIED~~ | **CLOSED as SUBSUMED, not built.** The verdict is now DEFINED in ADR `0057:66`, and ADR `0059` binds it: `0059:60` records that 0057 is why *durable memory* is MISCLASSIFIED, `0059:80` names the full table (**TRUE / FALSE / ASPIRATIONAL / MISCLASSIFIED / INSTRUCTION** -- five now, INSTRUCTION was added for unfalsifiable directives), `GAP62_PROMPT.md` is the re-audit standard, and `check_mind_prompt.py` gates the prompt against re-teaching what the verdicts removed. The row asked for the verdict to exist and matter; it now has a definition, an owning ADR, a standard and a gate | — | closed as subsumed |
| 82 | ~~Should MIND state survive a restart?~~ | **DECIDED 2026-09-02: YES -- KIND is not DURABILITY.** ADR 0057 *ephemeral nondeterministic inference state* names the **kind** (not application state, not metadata), not the lifetime. Durability is a separate axis, and stop/restore via KV machinery is the stated roadmap. `mind-nooa-data` as a NAMED volume is therefore CORRECT as-is: survives restart, dies on `down -v` | what needs fixing is PROSE that reads *ephemeral* as *must not persist*. Audit for it -- and note `check_mind_prompt.py` pins the MIND prompt, so any wording change there is a pin update requiring re-audit against `GAP62_PROMPT.md` (ADR 0059). Row 46 named-volumes-everywhere does not disturb this | **audited 2026-09-03: prose already correct** — `mind_agent` frames kind-vs-lifetime explicitly, no "must not persist" language anywhere in mind/compose, pinned prompt clean | closed |
| 83 | ~~Cache identity is unknowable from where MIND sits~~ | **DECIDED 2026-09-02: THE ROUTER OWNS REUSE.** SwitchYard decides when a cache may be reused, because it is the only component that knows which provider actually served a request -- MIND names no model, so the knowledge cannot live there. This concentrates the decision where the facts are | **it also makes the router responsible for inference correctness**, which is a new charter obligation on row 11 and must be written into that build, not assumed. The identity components Manus listed (weights, tokenizer, chat template, sampling config, adapter, cache format) are still **not established**; the router must supply what it can attest and REFUSE reuse for what it cannot. Do not let *the router owns it* become *the router guesses* | **dormant, tripwired** — no block-reuse consumer exists anywhere controlled (measured [`GAP11_CACHE.md`](../archive/findings/GAP11_CACHE.md)); the obligation binds the day one appears, enforced by `check_no_kv_cache.py` failing on arrival | closed |
| 84 | ~~There is no general stale-block detector~~ | **DECIDED 2026-09-02: a CACHE-ADMISSION CONTRACT -- prevention at the point of consumption.** A block may be CONSUMED only if its recorded execution identity matches the current one; no match, no reuse, and the miss is recorded as a refusal. Detection after the fact is impossible (SHACL validity, checksums, deserialization and fluent output all pass on a stale block), so the control is admission, not inspection | binds the **consumer**, not the artifact -- which is exactly what row 85 asked for, so **85 is substantially answered by this**. Doctrine must also state plainly that our SHACL gates do NOT catch plausible-but-incorrect inference; a cache hit is a risk-bearing admission, not proof of correctness | **dormant with 83** — same tripwire, same reactivation | closed |
| 85 | ~~A KV cache is not a participant, but needs participant-like gates~~ | **ANSWERED by row 84 decision.** The row asked that the BUS-imposes-change rule bind the **CONSUMER** and a cache-admission contract, not the artifact. That is precisely what was decided: a block may be consumed only if its recorded execution identity matches, and a mismatch is a refusal. ARTIFACT COMPATIBILITY now has its explicit form -- a superseded block may remain on disk for diagnostics and simply fails admission | dormant with 83; no separate decision needed | closed |
| 86 | **LOG: OTLP transports, CPCP governs** | Manus 2026-08-31d, given permission to reject CPCP as transport and taking it. Ordinary log volume must NOT go through the per-record admitted-operation path; CPCP supplies method/schema identity, admission policy, refusal semantics, receipt rules | five stages, local append BEFORE any network — stage map in [`ROW86_87.md`](ROW86_87.md): stage 1 exists + measured, stages 2–4 need a sink choice (owner), stage 5 explicitly unbuilt | **decided, unbuilt** |
| 87 | ~~LOG does not close ADR 0054~~ | **DECIDED 2026-09-02: LOG is an AGGREGATION PATH; the outside observer stays explicitly UNNAMED.** LOG permitted claim is the weaker one -- it helps WHEN THE SHARED LINEAGE IS HEALTHY and cannot evidence that the lineage FAILED. Row 101 sharpened why: the minimum outside observer is **WATCH plus RETAIN in a failure domain that is not the refuser**, and CI is provably not it (it observes the mechanism, not production). Leaving the observer unbuilt is more honest than letting LOG appear to close 0054 | build LOG as aggregation only, and write the weaker claim INTO it so a later reader cannot mistake it for the final observation point. ADR 0054 last link stays *not established* | **decided, unbuilt** |
| 88 | ~~We are inventing an OTEL-to-SHACL vocabulary~~ | **DOES NOT ARISE.** Ruled: do not reformat OTEL; SHACL only for required NEW metadata; prefer none. An OTEL LogRecord stays native and untranslated, so there is no translation layer to author or own | new metadata means a new attribute KEY, not a new format -- SHACL constrains values of keys we define | closed |
| 89 | ~~The failure-domain test is not premature~~ | **CLOSED as a measurement, not a fix.** Restart, chmod, first-write: both invariants hold. Directory gone: both fail (the floor is the file; recreate looks like a healthy short log). SIGKILL: no torn line observed (write finished first). Disk full: APFS RAM disk still had ~1MiB after ENOSPC on a neighbor file — not a zero-byte disk; JSONL still wrote. Findings: [`GAP89_FLOOR.md`](../archive/findings/GAP89_FLOOR.md) | — | closed as measured |
| 64 | ~~A fallback defeats its own check~~ | **CLOSED.** Parse errors are `:unparsable_frontmatter` (now reachable). Empty YAML is no-fields, not corrupt. Non-Hash is `:frontmatter_not_a_mapping`. Ingest is the only runtime reader of the symbol. Findings: [`GAP64_65.md`](../archive/findings/GAP64_65.md) | — | closed |
| 65 | ~~A failed idempotency store looks like a success~~ | **CLOSED without mounting sqlite.** Live store is MemoryIdempotency. Once-per-process `idempotency_not_durable` on RefusalLog. `get` stays nil-on-failure. Sqlite still not mounted; mounting list is in [`GAP64_65.md`](../archive/findings/GAP64_65.md) for persist (39/43) | — | closed |
| 66 | ~~shapes-level-8 packaged but unreachable~~ | **CLOSED. The CATALOG was wrong, not the manifest.** The manifest already named 13 P11 protocol NodeShapes runtime with source_shape in shapes-level-8, while ProfileCatalog defined L8 and never used it, against its own comment: Ownership from the binding manifest, do not re-decide here | now resolves them; gated by check_catalog_manifest.py | closed |
| 67 | **MY ROW WAS WRONG, NOT comments only** | P9 canonical vs L8 bundle also differ in sh:ignoredProperties: legacy carries rdf:type PLUS six governed fields, packaged carries rdf:type ALONE. Under sh:closed true the packaged shape is strictly STRICTER. I recorded same-enforceable-constraints and repeated it | **the fix I warned against would have HIDDEN this** -- a comment-insensitive hasher converges the two and buries a real closed-shape delta. Recorded in packaged_vs_legacy.json; gate NOT weakened | closed |
| 90 | ~~A consumer resolves from a gem it does not declare~~ | **CLOSED.** `rails-osi-level-8.gemspec` now declares `shapes-level-8` directly. `resolve` allowlists `SHAPE_GEMS` and refuses any other name; repo_root fallback is only for that list, and is not a declaration. Gate: `check_shape_consumer_deps.py` (2 consumers examined). Plant: undeclared consumer fails; empty CHECK_ROOT fails | — | closed |
| 91 | ~~The protocol engine depends on the application contracts~~ | **DECIDED 2026-09-02: ACCEPTED -- `rails-osi-level-8` is the Rails BINDING, not the protocol definition,** and a binding may legitimately depend on both the protocol and the application it binds. The measurement stands: **74 of 87** catalog entries resolve into `contracts/mind-pod`, 13 into `shapes-level-8`. Revisit only when a second application actually exists and demands it | **record it explicitly so the NAME stops implying otherwise** -- a reader seeing `rails-osi-level-8` reasonably expects protocol-only, and that expectation is what made this look like a defect. No refactor now | **recorded** in the gem README status line (binding, not protocol definition) | closed |
| 92 | ~~The ADR corpus does not validate against its own ingest~~ | **CLOSED.** Extended `SUBJECT_KINDS` with topology/doctrine/data (same reason `repo` exists — do not mislabel). Did not reclassify. `:constraint` means empty `enforced_by`; filled it on the in-force ADRs that named none, did not weaken `expect(breaks).to be_empty`. Gate: `adr-ingest.yml` runs the full mmg-adr suite. Findings: [`GAP92.md`](../archive/findings/GAP92.md) | — | closed |
| 93 | ~~Only one failure reason fails closed; four others still report applied~~ | **CLOSED.** `failed_envelope?` is any `{ok:false}`. The engine-limited SPARQL-star annotation DELETE is `rdf_star_annotation_delete` (a call site), not a reason allowlist. Immediate observes every SPARQL reason with restoration and does not mark applied. Gate: `check_projection_envelope.py` | — | closed |
| 94 | ~~The outbox was never installed -- nothing was ever enqueued~~ | **CLOSED.** Rehearsal on copies first (96 notes, table absent; `connected?` mis-reported `:check_failed`; after `connection` fix, drain 96/0, 384 triples). Live: `db:migrate:up VERSION=20260811170000` only (no `20260828*`), enqueue 96 pending, drain `{ok:true, drained:96, errors:0}`, **384 named-graph triples, 96 subjects, 0 applied-without-graph**. Gate: `check_outbox_migrated.py` (app migrate dir holds the gem copy). Did not fold U5. Did not `down -v`. Findings: [`GAP94.md`](../archive/findings/GAP94.md) | — | closed |
| 95 | ~~The call-site exemption still returns ok on a retract-only quoted-triple path~~ | **CLOSED.** `rdf_star_annotation_delete` still swallows engine-limited parse refusals, but returns `:graph_unreachable` so a retract-only quoted-triple DELETE cannot report success against a dead GRAPH. LATENT path; guard so a future caller cannot resurrect 7(b). Findings: [`GAP95.md`](../archive/findings/GAP95.md) | — | closed |
| 96 | ~~`enforced_by` is asserted non-empty, never resolved, and a doc reads like a gate~~ | **CLOSED.** Gate resolves every `enforced_by`/`stand_in` path and classifies enforcing / declaring / neither. In-force ADRs need an enforcing target or `unenforced: true` + `unenforced_because`. `stand_in` is a separate key and never counts as enforcement. 0058/0049/0050/0048/0051/0055/0057 declared unenforced (unbuilt). Did not invent gates. Findings: [`GAP96.md`](../archive/findings/GAP96.md) | — | closed |
| 97 | ~~`unenforced` is whole-ADR, so PARTLY enforced records itself as NOT enforced~~ | **CLOSED.** Chose (i) over `unenforced_for:` (a new key for one ADR). Partial is `unenforced: true` plus an enforcing target; whole is unenforced with none. 0057 now names `check_two_writers.py` (block style; shared with 0056) and keeps `unenforced: true` for BUS/MIND kinds. Gate fails a because that names `check_*.py` / "gated by" with no matching enforcing target — that is 0057-as-was. Did not drop the flag (would fake-enforce the unbuilt kinds). Did not invent BUS/MIND gates. Findings: [`GAP97.md`](../archive/findings/GAP97.md) | — | closed |
| 98 | ~~Catalog IRI and TTL NodeShape IRI can diverge silently~~ | **CLOSED.** Gate compares full IRIs: `ProfileCatalog::Entry#shape_iri` must equal a `sh:NodeShape` IRI in the TTL file the entry resolves. Local-name checkers unchanged. `check_iri_namespaces` still baselines catalog literals and `@prefix` separately — the join is this gate. Plants: catalog IRI change keeping local name; `@prefix` change keeping local names; empty CHECK_ROOT. Population: 87 catalog entries + 90 NodeShapes (6 files). Did not rename `osi.example`. 0044 `enforced_by` names the checker. Findings: [`GAP98.md`](../archive/findings/GAP98.md) | — | closed |
| 99 | ~~The MIND prompt is pinned; the AUDIT is still bound to nothing~~ | **CLOSED.** (a) pin landed `00fc0ea`. (b) new ADR 0059, not 0048 (unbuilt seam; stuffing the pin there would fake-enforce topology) and not 0057 (gap 97's partial; would add a second slice). `enforced_by`: `check_mind_prompt.py` + `mind-prompt.yml`. `GAP62_PROMPT.md` / `GAP99.md` cited in the body as the re-audit standard; docs are not `enforced_by`. Prompt untouched. 0048 and 0057 unchanged. Findings: [`GAP99.md`](GAP99.md), [`0059`](../adr/0059-mind-system-prompt-is-pinned.md) | — | closed |
| 100 | **MY gap-92 verification was partial -- it read only block-style YAML** | I reported *"102 enforced_by refs, 0 missing"* as evidence grok invented nothing. That awk matched only block style (`enforced_by:` then `  - path`). **13 ADRs use INLINE flow style** (`enforced_by: [a.py, b.py]`), 45 use block, 10 are empty -- so my sweep silently skipped every inline list, including `0044`, and reported a complete-sounding number. The CONCLUSION survives: grok `check_enforced_by.py` parses BOTH (block at line 59, inline at 60-65) and reports **137 examined, 0 missing**. So nothing was invented -- but it is the GATE that establishes that, not my manual check | recorded because a hand-verification that reads one syntax and reports a total is the same shape as the defects on this list | closed as measured |
| 101 | ~~Can the refusal floor detect its own loss from inside the container?~~ | **CLOSED as reasoned, not a fix.** After the files are gone, H1 (never ran) and H2 (had refusals, then `rm -rf`) produce identical files; no in-container reader of the floor can distinguish them. Not merely absent -- the floor IS the files. Minimum outside observer: WATCH a per-instance heartbeat and RETAIN last-seen seq outside the container filesystem. **CI observes the mechanism, not production incidents.** `fsync` changes none of the five gap-89 outcomes (directory-gone is unlink; SIGKILL-after-`write(2)` is already in page cache; fsync is a sixth, untested host-crash case). Did not patch the floor, did not add fsync, did not build LOG. Findings: [`GAP89_SELF.md`](../archive/findings/GAP89_SELF.md) | — | closed as reasoned |
| 102 | **A skip-list matched against ABSOLUTE paths silently skipped the planted file** | root cause of the gap-59 plant failure, found by grok: `check_cpcp_callers.py` `SKIP_DIR` contained `tmp`, and the test matched `part in SKIP_DIR for part in p.parts` on the **absolute** path. Plant `CHECK_ROOT` was `/tmp/gap59-*`, so `tmp` matched and `extra_client.py` was skipped -- the gate never saw the planted caller, `hit=False`, while a hand-plant inside the real tree worked. Fixed by making the skip **relative to CHECK_ROOT**. **Not systemic today:** four other checkers still test `in p.parts` on absolute paths (`check_language_rule`, `check_loopback_env`, `check_oxigraph_env`, `check_admission_status_absent`) but none of their skip lists holds a token that collides with a temp path, and all four plants pass with `hit=True` | the PATTERN is fragile -- it depends on no skip token ever colliding with a temp-path component. A plant that skips its own planted file reports *the gate failed* for the wrong reason, which is indistinguishable from working | **fixed at the pattern level**: every `in *.parts` skip match now runs against `CHECK_ROOT`-relative parts (language_rule, loopback_env, oxigraph_env, admission_status_absent, cpcp_callers/urlopen family, base_digests, closed, indeterminate, no_sentinels) — an absolute temp prefix can no longer trip a skip token | closed |
| 103 | ~~My sweep under-counted the gates and tested them by grepping output~~ | **CLOSED by running it properly.** `rdflib`/`pyshacl` are now installed at the CI-pinned versions (`rdflib 7.6.0`, `pyshacl 0.40.1`) in `.mm_tmp/venv-shacl` -- project-relative and gitignored, since Homebrew python is EXTERNALLY-MANAGED. The four shape checkers I reported as *skipped, not passed* all session now run, and **all four PASS**: constraint-ledger (865 pairs, 617 identical, 239 only-runtime, **9 conflict, 0 touching a bound_runtime shape**), drift (24 compared, 0 drift), quarantine (171 NodeShapes, 65 unowned, 46 unreferenced), runtime-artifact (7 wrap sites, 14 shapes, 0 divergences). **Two corrections to my own reporting:** the tree has **38** checkers, not the 24+4 I quoted -- roughly 14 landed during this session -- and my sweep judged pass by grepping `OK` from the last line, which misreported `check_shape_drift` as failing because its final line is *wrote evidence/shape-drift.json*. Now judged by **exit code** | first complete verification of the session: **38 checkers, 0 failures; 14 plants, 0 failures.** Reproduce with `python3 -m venv .mm_tmp/venv-shacl && .mm_tmp/venv-shacl/bin/pip install -r tooling/shacl/requirements.txt` | closed |
| 104 | **A non-200 refusal loses its envelope AT THE CLIENT** | **CLOSED** `81ec602`. Server side never needed changing — vault already renders `json:` with `status:`, so 401/403/404 arrive WITH `{ok:false, reason:, because:}`. The loss was `urllib.request.urlopen` RAISING `HTTPError`, whose body a `with`-block never reads. Fix: catch `urllib.error.HTTPError` and `e.read()` at each site — **no shared helper**, argued rather than assumed (it would create an import edge between `gems/rails-cpcp/front` and `runtimes/mind-pod` costing more than it saves). **`URLError` is deliberately NOT caught**: it is `HTTPError`’s parent, so collapsing them would swallow the far side’s reason along with genuine connection failures — the `failure_layer` distinction row 109 froze. Freeze file updated: `python_break` and `python_tests_break` both emptied into `python_read_body` (5 files); `implemented:false` and `default_profile: legacy-all-200` untouched. Gate: `check_urlopen_envelope.py` (7 sites, 0 skipped); plants `bare-urlopen-fails`, `urlerror-collapse-fails` | — | closed |
| 105 | **Manus: the dual-signal ruling is PRINCIPLED; 21 boundary failure modes named** | [`2026-09-02a-layer-reconciliation-manus.md`](../archive/reviews/2026-09-02a-layer-reconciliation-manus.md). **Verdict on row 49: principled, not incoherent.** JSON-RPC 2.0 is transport-agnostic and prescribes NO HTTP status mapping; *always-200* is a deployment convention, and the belief traces to a **JSON-RPC 1.2** extension proposal, not 2.0. **Gap 104 is an ADAPTER defect, not evidence the server is wrong** -- fix the client, do not weaken the refusal signal. **vault REST -> CPCP is a REAL binding change, not a rename:** safe retrieval, URI identity, conditional requests, representation metadata, cache control and standard auth signalling do NOT automatically survive. **Shape-only IRI identity is a sound stopping point**, with the honest caveat that W3ID is durable redirect + stewardship, NOT a guarantee of permanent availability. **Hydra is an unofficial draft, not a W3C Standard; LDP is a Recommendation** -- neither is implied by using JSON-LD + W3ID | delivers an **ADR-ready 4-layer authority table** (HTTP / CPCP / REST binding / W3ID-RDF), each with what it must NOT decide and a required seam declaration -- directly expressible in row 20 `seam_authority.json`. Plus **21 failure modes** in server-correct / information-destroyed / preventive-control form. Names the FOUR conditions under which dual signalling becomes incoherent | **expressed**: 4-layer authority + prohibitions live in `seam_authority.json` (`layers`), every live seam declares its `bindings`, gated by `check_seam_authority.py`; failure modes stay in the review | closed |
| 106 | **Manus: LEAD NARROWLY, FOLLOW BROADLY -- publish 3 classes, not an ontology** | [`2026-09-02b-lead-narrowly-operation-identity-manus.md`](../archive/reviews/2026-09-02b-lead-narrowly-operation-identity-manus.md). Owner framing accepted: **application = DOMAIN, MagenticMarket.ai = ECOSYSTEM, and only BOUNDARY-CROSSING methods get published**. Four conditions for leading, all met -- live cross-domain use case, semantics not expressible by JSON-RPC names/codes alone, team can commit to IRI persistence, and the vocabulary stays small. **Leading a general operation ontology, app-store standard or hypermedia framework is NOT justified.** Follow JSON-RPC 2.0, JSON-LD and SHACL; do not adopt Hydra by implication. **Minimum first release: `osi:Application`, `osi:Operation`, `osi:RefusalReason`** plus the links actually present in the wire contract -- `Capability` and `Offer` DEFERRED unless current messages already exchange them. A six-family CEILING, not a target | **the boundary must be a MANIFEST, not a naming convention** -- `operation_iri, wire_method, direction, producer_domain, consumer_domain, request/response_shape, refusal_reasons, profile_versions`, with exactly two endpoint roles and **no wildcard all-services role**. Four CI checks including a planted-negative that must fail *if the checker is disabled, if its input path is empty, or if it merely checks exit status without inspecting the violation* -- our own plant discipline, stated back to us. Honest limits: **NOT ESTABLISHED** that any third party adopts CPCP; CloudEvents succeeded but had CNCF governance, 340+ contributors, 122 orgs -- *conditions MagenticStack does not currently have* | **manifest half built**: all 12 served methods manifested (iri, wire, parties, shapes, refusals, profiles[]) and gated both ways by `check_boundary_manifest.py`; ontology publication (3 classes + W3ID pledge) stays an owner act, fenced in the manifest | closed |
| 107 | ~~The sole boundary message is NAMED but NOT MODELLED~~ | **CLOSED as shapes.** `cf:ContextFrameShape` / `MeaningShape` / `ClarificationShape` in `shapes-level-8/bundles/contextframe.shacl.ttl`. Containment: `inContextFrame` / `inMeaning` minCount 1. Namespace `https://w3id.org/cpcp/osi8/contextframe#` (0060 convention; no w3id redirect published). Not p8 FrameChange, not ux:PanelFrame, not profile-11. No CPCP wrap yet. Gate: `check_contextframe_containment.py`. Findings: [`GAP107.md`](../archive/findings/GAP107.md) | wrap as a CPCP operation is later | closed |
| 108 | ~~Row 15 collides with the read-back asymmetry~~ | **CLOSED as measured.** Split by credential need: `catalog.mjs` 96, no imports, no secret -- legal on `ROLE=config`. `discovery.mjs` 78 reads `state.keys` and egresses with the token. `verify.mjs` 77 takes `complete` from `server.mjs`, which uses those keys. Today's keys are switch `/state`, not vault. Did not port. Did not add `get`. Did not invent a config-to-switch probe RPC. Findings: [`GAP108.md`](../archive/findings/GAP108.md) | owner places discovery+verify | closed as measured |
| 109 | ~~Manus status mapping: our instinct was RIGHT, and one boundary is NOT ESTABLISHED~~ | **CLOSED as planned.** Dual-v1 contract freeze: 200 only for a completed RPC whose result is a domain decision. **Grounding is 200**, not 422 -- in this tree `grounding_refused` is the method completing after SHACL of the domain payload; 422 is reserved for request-document SHACL before dispatch (no live producer). Admission (`authorization_denied`) stays 200; its `failure_layer` is the sentinel `owner` (never a wire value) -- whether that layer is domain vs `http_auth` remains an owner call. `failure_layer` designed on `error` next to reason/because. 503 is not a general retry; `Retry-After` only with a known window and safe replay. Rollout: `CPCP_HTTP_STATUS_PROFILE=dual-v1`, per-method opt-in, not a controller-wide flip. RpcController still `status: :ok` at four sites (`implemented: false`). Python urlopen is still gap 104. Gate: `check_cpcp_http_status.py`. Findings: [`GAP109.md`](GAP109.md) | mapper behind the flag after gap 104 | closed |
| 110 | ~~`check_shape_quarantine` population is PINNED to 171~~ | **CLOSED.** Census is in-scope NodeShapes (scope sweep + binding manifest + graph), not `shape_owner_candidates.json`. A live name missing from the inventory fails. Empty CHECK_ROOT fails. `--write` put ContextFrame/Meaning/Clarification into the inventory (unowned, self_targeting). Gate: `check_shape_quarantine.py`. Plants: extra NodeShape; empty root. Findings: [`GAP110.md`](../archive/findings/GAP110.md) | — | closed |
| 111 | **MY sweep never ran the Ruby plants** | I have reported *N plants, 0 failures* on every landing this session using `find tooling -name plant_*.py`. There are also **4 `plant_*.rb`** -- `plant_projection_unreachable.rb`, `plant_two_writers.rb`, `plant_refusal_floor.rb`, `plant_restoration.rb`. So my population was **20 of 25**, and I reported a confident total over it. grok reported 25 and I could not reconcile the numbers, which is how it surfaced | same class as gap 100 (`enforced_by` block-style only) and gap 102 (skip-list on absolute paths): **a pattern that silently under-matches, producing a real number over the wrong population**. Third instance in my own verification. Sweep is now `plant_*.py` AND `plant_*.rb`, run as two passes because the substrate times out past ~30s | closed as corrected |
| 112 | ~~`plant_projection_unreachable.rb` fails on main -- gap 69 moved the refusal that gets recorded first~~ | **CLOSED.** Diagnosis confirmed: Immediate refuses on `schema_status` before GRAPH; no-connection is `:check_failed` / `outbox_schema_check_failed`, not `:missing`. The plant now has three fixtures: no AR connection (`outbox_schema_check_failed`); AR up, no table (`outbox_not_installed`); `ensure_schema!` then SPARQL fail (`graph_unreachable` / `sparql_parse_error`). A GRAPH stub is not recorded when the outbox is missing. Did not change Immediate. Did not accept any reason. Gate: `check_outbox_schema.py` fails if the plant drops one of the three named cases. Findings: [`GAP112.md`](../archive/findings/GAP112.md) | — | closed |
| 113 | ~~Row 46 scope is narrower than when it was decided: it is ONLY the two credential stores~~ | **CLOSED as analyzed.** Neither store is SQLite (`secrets.json` encrypted JSON; `sources.json` JSON). Virtiofs/SQLite hazard does not apply. Recommend convert neither; amend row 46 (exception), keep 0046:85. Gate: `check_credential_bind_mounts.py`. Findings: [`GAP46.md`](../archive/findings/GAP46.md) | owner on row 46 | closed |
| 114 | ~~A held branch reached `main`~~ | **CLOSED** `1e571e4`. A `core.hooksPath` pre-push hook refuses a push to `main` unless `bin/sweep` is green — chosen over CI-only because CI lands *after* the push, which is exactly too late for `cd1166a`. **Fail-closed is the substance**: missing rdflib, missing ruby, wrong LANG or a timeout is a FAIL, never a skip; zero jobs is a fail; and the sweep fails if it finds 0 `plant_*.py` OR 0 `plant_*.rb` (the gap-111 lesson made a gate). **86 jobs.** Exclusions are NAMED, and `because` must cite the workflow that still runs the job — a ghost exclusion path fails (`ghost-exclusion-fails`), which is gap 102 defended. Hatch is `MM_SWEEP_OVERRIDE=because:<reason>` with reason ≥ 20 chars; `true`/`yes`/`1` are refused as “a flag, not a because”, and `silent-hatch-fails` gates that. `main-green.yml` is the net for `--no-verify` | — | closed |
| 115 | ~~Superseded ADR text reads as live~~ | **CLOSED** `8f4362d`. Every sentence quoted in an amendment’s Withdrawn list must now be **struck at its source line** with a pointer to the amendment — `0050:38` and `0050:131` are struck, the first with an anchor link. Gate: `check_withdrawn_quotes.py`, **12 examined 0 skipped, 5 amended ADRs, 7 quotes**. Two plants cover the two ways it could rot: `unstrike-fails` (someone removes a strike) and **`unmatched-fails`** — a quote the checker cannot locate at an origin is a FAIL demanding a manual mark, never a silent pass. Amendments with no Withdrawn list are **examined (0 quotes), not skipped** — 0052 and 0055 report that explicitly. This is the first rule here about whether a decision’s TEXT is still true; `enforced_by` only ever gated whether it is ENFORCED | — | closed |
| 117 | **There is no proven actor, so the pod cannot name a viewer** | ADR [0040](../adr/0040-the-session-is-one-entity.md) decided this and said so: `Vv::Base::Session#actor_id` is "nullable, unconstrained, and asserted by whoever opened the session", `session.open` returns `actor_proven: false`, and the model's own comment says a Session identifies a **scope, not a principal**. No identity gate sits in front of the pod — nothing authenticates, so no ROLE can attribute a request to a person. Measured 2026-09-13: unchanged since 0040 was written. For contrast, Cloudflare OS ships two paths (built-in username/password, or `AUTH_GATEKEEPERS` sign-in keyed on verified email) — [`CloudflareOs_Contrast.md`](CloudflareOs_Contrast.md) §3 | ADR [0070](../adr/0070-never-persist-datasets-and-the-inverted-observer-seam.md) **cannot be satisfied**: its decision 2 is a per-viewer read with that viewer's own credential, which needs a principal, so it fails closed on `actor_unproven`. Also P6 authorization-evidence, which 0040 states a session does not satisfy. Also [`plan_sharedai_canvas.md`](plan_sharedai_canvas.md), which may not claim 0070's guarantee until a viewer can be named — one shared credential serving two viewers is the failure mode, not a first step. Also **delegation-chain re-binding** (BoundaryGaps gap 5, section 6): RFC 8693 re-binding proves *on whose behalf*, and there is no whose until this row closes -- which is why gap 5 gets no row of its own. **Owner unnamed** | prerequisite |
| 121 | ~~The container count is unreconciled~~ **ANSWERED — and it already was** | The count is **14**, and `tooling/governance/check_doc_counts.py` has counted it from `runtimes/mind-pod/app/extract/compose.yml` since 2026-09-15 (ParallelAgentWork §Decided). Four documents — `README.md` twice, `docs/architecture/OVERVIEW.md`, `docs/README.md` — already assert 14 and gate green. What was wrong is narrower: **this file and `ContainerTopology.md` were never registered in `CLAIMS`**, so nothing checked them and they drifted to 12. Recording this row as an *owner decision* was itself the error — the owner had decided and a gate was enforcing it | both are registered as of 2026-09-17. `SWITCHYARD.md` and `pod-to-vps.md` stay **unregistered**: their 12 is a dated decision premise and a verbatim quotation, true when written | closed |


## 2b. DB_PATH as a CPCP effect (ADR 0051)

| # | Gap | Measured today | Blocks | State |
|---:|---|---|---|---|
| 39 | ~~The path parameter must be a CLOSED set~~ | **CLOSED.** Declared in `tooling/compose/store_bindings.json`: three sqlite paths (domain, bus, mind) with volume, env var, and rw/ro writer-sets. The free-form env string is gone as a possibility — any new path fails the gate. Findings: [`GAP39_43.md`](../archive/findings/GAP39_43.md) | persist serves effects against this set when built (row 8) | closed |
| 40 | **Boot-time resolution; a change is a reconnect** | `database.yml` evaluates `ENV.fetch("DB_PATH")` in ERB at load; consumers also in `entrypoint.sh` (x2) and `spec_helper.rb`. **CORRECTED 2026-09-03 by [`ROW41.md`](../archive/findings/ROW41.md): this is true of Rails, not of MIND.** `mind_agent.py:67` reads the env inside `_storage()` on every `build_agent`, called per cognition cycle — so the pod does NOT have one uniform boot-only binding | **answered**: restart-per-kind is ROW41 §3, the per-cycle reader is ROW8 §3, in-flight rule is S2 (no operation spans the swap) — the semantics exist distributed across those rulings, no separate rule needed | closed |
| 41 | ~~The bootstrap paradox~~ | **SCOPED** — [`ROW41.md`](../archive/findings/ROW41.md) `89ef35e`. Decision stands (boot-only swap, refuse a live swap). **Update post-GAP8:** 0051's control plane HAS a server now — `ROLE=persist` serves `persist.path.set/get`, records next-boot intentions, refuses open-set paths structurally never live. A BACK always-refuse method would still **fake-enforce** 0051. **Two findings the row did not carry.** (a) “all consumers are boot-only” is FALSE: Rails is ERB-at-load, but MIND’s `_storage()` re-reads `DB_PATH` on **every** `build_agent`, and `harness.py:114` calls it each cognition cycle — it would follow a mutated env with no restart. (b) **declining RES (row 17) broke this row’s continuity argument**: “continuity comes from the event log” now names BACK’s operation journal, which lives *inside the sqlite being left behind*. Not a reason to reopen live-swap — a reason persist must hold placement OUTSIDE that file | build the caller wiring and first placement on the existing persist seam; the applying restart stays operational | **closed as rehearsed**: record → restart → rebind proven on isolated fixtures (marker survives, placement recorded, BACK rebinds); a production placement is operations, not gaps | closed |
| 42 | ~~MIND has a SQLite it does not bind~~ | **DONE.** `DB_PATH` binds `SQLiteStorageManager`; unset keeps NOOA's `:memory:` default. Named volume `mind-nooa-data` in both composes | — | closed |
| 43 | ~~Row 1 becomes reachable at runtime~~ | **CLOSED as gated, not as built.** `check_store_bindings.py` enforces the declared writer-set per path (50 examined): a compose edit pointing two writers at one file — or any role at a fourth path — fails the sweep instead of reaching runtime. Gate: `check_store_bindings.py` (plants: rogue-path, back-binds-bus, missing-bus-volume). Findings: [`GAP39_43.md`](../archive/findings/GAP39_43.md) | 0051 §5 amended to writer-set language | closed |
| 44 | ~~Does `ROLE=persist` survive?~~ | **CLOSED. Yes.** Placement authority is a job it can do without a database server — which is what "persist owns physical storage" foundered on | — | closed |
| 45 | ~~NOOA already enforces single-writer~~ | `_acquire_session_lock` + `SessionAlreadyActiveError` still guard the mind file from inside NOOA (no action needed) | **SUPERSEDED**: row 43 resolved via declared writer-sets + sweep gate instead of lock adoption; there is nothing left to adopt | closed |
| 46 | ~~A path is a file triple, on a hazardous mount~~ | **AMENDED 2026-09-02: credential bind mounts are THE EXCEPTION. Convert neither.** The original ruling (*named volumes everywhere*) was made when the scope read as a general mount-type question. Measured since: **every non-credential store is ALREADY a named volume** (`mind-data`, `mind-nooa-data`, `graph-data`), leaving only `.agent/vault:/vault` and `.agent/secrets:/state` -- **both credential stores** (gap 108). And the hazard does not reach them: `_is_virtiofs` lives in `nooa/storage/sqlite.py`, takes a `db_path` and handles `:memory:` -- it is a **SQLite** detector. **Vault is encrypted JSON** (`store.rb:74-76`, `binwrite(tmp, encrypt_and_sign(JSON..))` then `File.rename`); **switch is `sources.json`**. No `db`/`-wal`/`-shm` anywhere. So the SQLite half of this row was already done and the remaining half would spend a real property to buy protection against a hazard neither store can have | **ADR 0046:85 stands unamended** -- *keys must survive `docker compose down -v`* remains in force and remains satisfied. `check_credential_bind_mounts.py` gates it, so the safe state cannot drift. Cost that was avoided, costed WITHOUT reading either store: vault slots are operator-named and unrecoverable from inside the pod (read-back asymmetry; the master key does not resurrect a deleted file), and switch holds **five** key slots plus enabled/prices/discovered/verified -- so a `down -v` would have lost the discovery and verification work too, not only keys | **closed as amended** |
| 47 | ~~MIND's system prompt becomes false~~ | **DONE.** Prompt rewritten and the store bound in the same change: "you never persist anything" -> a WHAT YOUR OWN MEMORY IS, AND IS NOT section; memory is private and is not evidence | — | closed |
| 48 | ~~Is persist the placement authority for EVERY store, or only the event log?~~ | **DECIDED 2026-09-02: EVERY store.** The event log, the Rails domain SQLite, and MIND NOOA memory. This ratifies what was already stated -- *mind-nooa-data is persisted at a path determined by PERSIST* -- rather than extending it. Placement and ownership are different axes: ADR 0057 three KINDS of state still hold, and `persist` deciding WHERE does not touch WHO writes | **closed as served**: persist serves set/get over all four closed-set stores; the writer-set invariant is refused at record time (off-set paths) and at deploy time (sweep gate) — the only two senses a placement authority can enforce it | closed |

## 3. Outside the language rule

| # | What | Count | Status |
|---:|---|---:|---|
| 25 | Python release gates under `tooling/` | 23 | **carve-out adopted**: "`tooling/` is Python until rewritten" |
| 26 | Python validators in `gems/osi-level-8-profiles` | 4 | same carve-out |
| 27 | Shell: `bootstrap`, `build-baselines`, `docker-containers`, `prereq` | 4 | outside all three languages |
| 28 | CI workflows (YAML) | 11 | not covered; they **invoke** the Python gates |
| 29 | `runtimes/effect-plane` | 1 gem | no container assigned |
| 30 | `mmg-blob` / `vv-blob` | 2 gems | blob storage has no owner |

## 4. Browser carve-out (not violations)

| # | What | Why JS |
|---:|---|---|
| 31 | `gems/switchyard-offline` | Chrome MV3 extension; the one gem with **no gemspec** |
| 32 | `gems/vv-html-components` | web components |
| 33 | `rails-osi-level-8/.../ux-host-layout.js` | browser asset |
| 34 | `config-admin`'s UI, once it exists | runs in a browser |

## 5. Accepted costs and dead prose

| # | Item | Source |
|---:|---|---|
| 35 | Hot-patch granularity is 4 units, not 12 | 0047 amendment 1 |
| 36 | ADR 0046 keeps its network/mount/allowlist boundary but loses image isolation | 0047 amendment 1 |
| 37 | `mind_agent.py:58` "There is no second socket, and there is no write path for you" — now false | 0048 |
| 38 | Every "`/_cpcp` is the ONLY write path" comment — now false; means *BACK is the only writer of domain state* | 0050 |

## 6. Boundary gaps (survey-derived, unscheduled)

Rows 118-120 come from `docs/review/BoundaryGaps.md` in the **magentic-market-ai**
repository -- a substrate-side review of CPCP x magentic-stack dated 2026-09-16 --
not from a measurement of this tree. They are recorded so the five field-wide gaps
can be cited by number, and so their absence from the critical path is a stated
decision rather than an oversight. Every row here is **survey-derived**: no
`file:line` evidence, no gate. That is precisely why none of them is `open`, and
why they sit in their own section instead of being interleaved into sections 1
and 2, where every row is measured.

**The goal that keeps them off the path** (owner, 2026-09-16): a
**Perch/CPCP-centered flow** -- one seam, one approval surface, one journal,
carried through the entire lifecycle, to reduce churn and reduce perplexity.
Each row below is a departure into a different discipline: an information-flow
interpreter, a second approval surface, a confidential-compute profile. Adopting
one is a decision to widen the centre, not an increment on it.

Disposition of all five gaps, so the mapping is not re-derived by hand.
**Coverage** here means a measured, scheduled row in sections 1–2. Citation
numbers in this section (118–120) are not coverage.

| Gap | Coverage | State |
|---|---|---|
| 1 Malicious-but-well-formed | none | stays — never scheduled |
| 2 Identity | **117** | no proven actor, not “VAULT unverified” |
| 3 Approval fatigue | none | stays — never scheduled |
| 4 Operator trust / TEE | none | stays — never scheduled |
| 5 Delegation chains | none | blocked on 117 |

Rows **10** (CI wiring of the MIND seam battery) and **86 / 87** (LOG stages 3 and 5) stay **out of this table**. They are leftover coverage. They are not these five.

118 / 119 / 120 below are citation numbers for the three unscheduled gaps so their absence from the path is citable. They are `carve-out or unowned`. They are not Coverage.

| # | Gap | Measured today | Blocks | State |
|---:|---|---|---|---|
| 118 | **Planner information-flow labels, and a receipt the Gate can read** | Measured 2026-09-16: `CaMeL`, `FIDES`, `taint` and `information flow` appear **0 times** in this file and no gate implements them. One adjacent rule does exist and is gated, but it is not this one: ADR 0064 keeps refusal evidence at `ledger_placement: "private_local"` because a refused payload never passed its shape and is therefore unvalidated caller-controlled input; `tooling/osi/check_refusal_registers.py` enforces it (829 Ruby files, green) and `plant_refusal_registers.py` plants the promotion out of `private_local`. That is **one hand-written flow rule over one payload class**, not per-value labels a planner propagates. BoundaryGaps named this seed `PrivateLocalLedger` / `PrivateLocalArtifact`; **no such classes exist** -- `private_local` is a ledger placement value, one of three (canonical, sync_intent, private_local) | nothing scheduled | carve-out or unowned |
| 119 | **Approval fatigue has no measure here, because there is no approval surface to measure** | Measured 2026-09-16: `HITL`, `approval` and `Effect Gate` appear **0 times**. `envelope` appears 7 times and **every one is the never-raise `{ok:false, reason:, because:}` envelope** -- not the Perch signed **approval envelope**, which is a different object that happens to share the word. Perch envelope mode (`per_instance` / `envelope` / `simulated_only`) is designed in the Perch docs; `vv-perch` ships schema only (R1-R3: no Gate, no ledger, no signatures). No approval rate is instrumented, because nothing prompts | nothing scheduled. FRONT is the named surface if one is ever built -- MIND owns no UI | carve-out or unowned |
| 120 | **Operator trust is a deployment profile nobody has asked for** | Measured 2026-09-16: `confidential`, `attestation`, `Firecracker`, `microVM` and `krun` appear **0 times**; the 4 case-insensitive hits for `TEE` are all `guarantee` and `sixteen`. Containers are shared-kernel Docker -- a documented risk acceptance, not Firecracker/libkrun. MIND is distroless, which is cell hardening and **not** operator exclusion. Self-host is the entire trust story, and the host operator can read every volume | nothing. Correctly outside CPCP: a TEE profile is a deployment profile of this reference design, never a CPCP conformance class | carve-out or unowned |

## Critical path

The original path (rows 21, 4/50, 5, 6, 14, 16/18, 2) is **complete**.
What remains is not a sequence of container landings.

| Order | Do | Why |
|---:|---|---|
| 1 | **Row 117** — name an identity-gate owner, then build a proven actor | ADR 0070 cannot be satisfied without a principal. Owner unnamed. P6 evidence, Shared AI Canvas, and BoundaryGaps gap 5 wait on this. |

Not on this path: Gaps 1, 3, 4 stay unscheduled (§6). 117 is prerequisite. 121 is closed. Leftover work is groups 1–3 and 5. Group 4 is the stop list. Group 6 is dormant / later. Group 7 is amendment and operations debt. Group 8 is owner acts and publication. Group 9 is honest exclusions. Group 10 is observation leftovers. Group 11 is an undeclared seam and recorded shape deltas. Group 12 is superseded closed-row measurements. Group 13 is unenforced flags whose because names a landing as unbuilt. Group 14 is an ungated human pin, and facts the pin landing recorded. Group 15 is not this tree, or filling it here would lie. Group 16 is facts the FRONT compose swap recorded. Group 17 is the seventh image lineage, and facts the count fix recorded. Group 18 is the double-landing, and facts the merge recorded. Group 19 is prose the swap dated.

## Next leftover coverage

Not these five. Not 117. Stuffing leftover work onto the five-gap path is how it got mistaken for identity / IFC / HITL / TEE / delegation.

### Group 1 — coverage rows

Row 10 stays `open`; 86 and 87 stay `decided, unbuilt`.

| Order | Row | Do | Why |
|---:|---|---|---|
| 1 | **10** | CI-wire the MIND seam battery | slices 2–5 built; the runner exists ([`GAP10_CONF.md`](../archive/findings/GAP10_CONF.md) snippet). The workflow does not. Needs a runner to prove, not a YAML guess. |
| 2 | **86 / 87** | LOG stages 3 and 5 | stages 1, 2 and 4 exist ([`ROW86_87.md`](ROW86_87.md)). Stage 3 (CPCP LOG surface) deferred. Stage 5 (outside observer) explicitly unbuilt — CI is not it. |

### Group 2 — named leftovers, no coverage row

Do not invent rows for these. They have no State column because they are not population. A human pin, a deferred predicate, and a board measure are not coverage holes.

| Order | Item | Do | Why |
|---:|---|---|---|
| 3 | FRONT Bun pin | swap compose off `mind-pod:demo` onto `front-base` | `runtimes/front-base` and `FLOOR-FRONT.json` exist (ADR 0072). Saying FRONT is Bun of the running pod is a claim ahead of the deployment. Human pin, not auto-bump. |
| 4 | Perch `succeeding?` | do not build it | [`plan_vv-perch.md`](plan_vv-perch.md): done is released + reporting; succeeding needs the level the business owner declared at P4. Inventing a met sentinel fabricates the contract. |
| 5 | §7.6 entanglement | do not add a `perch_` table | a Release Board measure over slices, not schema. A sixteenth table would break the fifteen-table claim. |

### Group 3 — unowned gems, and a follow-up on a closed row

25–28 stay carve-outs by adoption (tooling Python, shell, CI YAML). They are not next. 29 and 30 are the ones that still need an owner. Do not invent a container to close them from this table.

| Order | Row | Do | Why |
|---:|---|---|---|
| 6 | **29** | name a container for `runtimes/effect-plane`, or record that it stays a gem with no ROLE | section 3: no container assigned. A Plane C gem without a writer is not a twelfth-container problem — the twelfth is nats. |
| 7 | **30** | name an owner for `mmg-blob` / `vv-blob` | section 3: blob storage has no owner. Persist places paths; it does not own the blob gem. |
| 8 | **116** follow-up | remove in-pod `expose: 3000` | row 116 is **closed**. The follow-up is named in its Gap cell. Not a new row. Not a reopen. |

### Group 4 — not next

Named so they do not get restated as leftover work. Each already has a state line. Do not schedule them. Do not invent a row.

| Item | Already | Why not next |
|---|---|---|
| **11** Node leftover | closed | gated named violation of the language rule. Rewrite-to-Rust is not a coverage increment; it is the day the pin moves. |
| **13, 23** | reframed | the row as written was the wrong question. |
| **25–28** | carve-out adopted | tooling Python, shell, CI YAML. Group 3 already excluded them. |
| **31–34** | no state by design | browser carve-out, not violations. |
| **35–38** | no state by design | accepted costs and dead prose. |
| Gaps **1, 3, 4** of the five | §6 unscheduled | never scheduled. Not leftover coverage. |

### Group 5 — closed-row follow-ups, and a container this file never numbered

Not a restatement of group 4. Not these five. Not 117. Do not invent a row number for rag from this table.

| Order | Item | Do | Why |
|---:|---|---|---|
| 9 | **6** follow-up | put `contextframe.shacl.ttl` and `profile-9-ghis.ttl` in `ProfileCatalog`, or keep `incomplete: true` honest | row 6 is **closed** as partial. Those two files are on disk and absent from the catalog. The row already names them. Not a reopen. |
| 10 | **109** follow-up | flip `CPCP_HTTP_STATUS_PROFILE=dual-v1` per-method, or leave `implemented: false` | row 109 is **closed as planned**. RpcController still `status: :ok` at four sites. Mapper behind the flag. Not a reopen of the contract freeze. |
| 11 | `rag` / `milvus` | **row 121 now records the count.** Do not pick 12 or 14 from this table | [`RagContainer.md`](RagContainer.md) still names rag the 14th. Row **121** is the owner decision: compose defines 14, header says 12, LOG is reserved as 13 and unbuilt. Group 5's "unnumbered container" half is answered. The write path stays group 6. |

### Group 6 — dormant / later

Not group 4 (never schedule). Not group 5 (follow-ups that can start). These already have a state. They **reactivate**; they are not work to begin from this table.

| Order | Item | When it binds | Why not start |
|---:|---|---|---|
| 12 | **83 / 84 / 85** | the day a KV-cache consumer appears | closed, **dormant, tripwired**. `check_no_kv_cache.py` fails on arrival. The router owns reuse; identity components are not established. Do not build a consumer to close the tripwire. |
| 13 | **22** | a later compatibility rename of `osi.example` | closed as resolver. Historical IRIs resolve on read. Rename is a later event, not a coverage increment. |
| 14 | **107** | wrap ContextFrame as a CPCP operation | closed as shapes. Containment is gated. "Wrap as a CPCP operation is later." Not a new shape. |
| 15 | `rag.write` | after rag has a section-1 row, and an owner decides upsert/delete | [`RagContainer.md`](RagContainer.md): `rag.upsert` / `rag.delete` already refuse `rag_write_undecided`. Group 5's count half is now row 121. This is the write-path owner call. Do not guess embedding here. |

### Group 7 — amendment and operations debt

Not group 4 (never schedule). Not group 5 (code follow-ups). Not group 6 (dormant tripwires). Closed rows that still owe an amendment, an operations act, or an owner fill. Do not invent a row. Do not guess `failure_layer`.

| Order | Item | Do | Why |
|---:|---|---|---|
| 16 | **17** | amend ADR 0050 | closed: RES declined because it would be a second event log. "ADR 0050 now owes an amendment: BUS is the seam + projection, not an event store." Dated record, not a silent rewrite. |
| 17 | **41** | production persist placement | closed as rehearsed. Caller wiring and first placement on the persist seam; the applying restart is operations, not gaps. |
| 18 | **65** | sqlite idempotency remains unmounted | closed without mounting sqlite. Live store is MemoryIdempotency. Mounting list is in [`GAP64_65.md`](../archive/findings/GAP64_65.md) for persist (39/43). |
| 19 | **75** | first versioned BUS consumer | closed: `CONTRACT_VERSION` and `check_bus_contract.py` exist. "Participant-side refusal activates with the first versioned consumer." Do not invent a consumer to close it. |
| 20 | **109** owner fill | `failure_layer` for `authorization_denied` | group 5 order 10 is the dual-v1 *flip*. This is the owner call the freeze left: the layer is the sentinel `owner`, never a wire value. Do not guess `domain` vs `http_auth`. |

### Group 8 — owner acts and publication

Not group 7 (amendment of an existing ADR, persist operations, first BUS consumer). Not group 6 (later wrap / rename / tripwire). These are owner *publication* and *placement* acts the closed rows already name. Do not invent a vocabulary. Do not publish W3ID from this table.

| Order | Item | Do | Why |
|---:|---|---|---|
| 21 | **106** | publish three classes + W3ID pledge, or leave them fenced | closed: manifest half built (`check_boundary_manifest.py`). "Ontology publication (3 classes + W3ID pledge) stays an owner act." `Capability` and `Offer` DEFERRED unless current messages already exchange them. |
| 22 | **108** | owner places discovery + verify | closed as measured. Catalogue is legal on `ROLE=config`. `discovery.mjs` and `verify.mjs` stay on switch (token holder). Did not invent a config-to-switch probe RPC. |
| 23 | **107** redirect | publish the w3id redirect, or record that namespace is convention only | group 6 order 14 is the *wrap*. This is "no w3id redirect published." Durable redirect is stewardship, not a shape. |
| 24 | **110** | own or drop the unowned / unreferenced NodeShapes | closed: census is live. 65 unowned, 46 unreferenced were the measured remainder when ContextFrame landed. A live name missing from the inventory still fails. |
| 25 | **8** | `live_applied:false` stays until a production placement | persist is closed as served. Intentions against the closed set, never live. Group 7 order 17 is the operations restart. This is the flag that must not flip in a gap. |

### Group 9 — honest exclusions

Not group 4 (never schedule as work). Not group 8 (publish / place). These are closed rows whose remainder is an *exclusion*, a *name*, or a *sixth untested case*. Do not add RestorationShape to the census to close 74. Do not rename the gem to close 91. Do not add fsync to close 101.

| Order | Item | Do | Why |
|---:|---|---|---|
| 26 | **74** | leave RestorationShape out of the OSI L8 census | closed: restoration object is gated. Exclusion is stated (171 in-scope, 5 exclusions). Putting it in the census would pretend a CPCP envelope field is an L8 shape. |
| 27 | **91** | leave the name; revisit when a second application exists | closed, **recorded** in the gem README: `rails-osi-level-8` is the Rails *binding*, not the protocol definition. 74 of 87 catalog entries resolve into mind-pod. No refactor now. |
| 28 | **101** | do not add `fsync` to close the floor | closed as reasoned. `fsync` is a sixth, untested host-crash case. It changes none of the five gap-89 outcomes. LOG stays group 1. |
| 29 | **109** 422 | do not stand up request-document SHACL to mint 422 | group 5 is the dual-v1 flip. Group 7 is `failure_layer`. 422 is reserved for request-document SHACL *before* dispatch; BACK has no such gate. `RequestBody` is empty/unparseable → 400. |
| 30 | **113** | leave 0046:85; owner already on row 46 | closed as analyzed. Convert neither credential store. Gate: `check_credential_bind_mounts.py`. |
| 31 | **121** | stop asking leftover tables to pick 12 or 14 | closed on main `a93d9d3`: the count is **14**, `check_doc_counts.py` already counted compose. This file and ContainerTopology were unregistered in `CLAIMS`. Group 5 order 11 is answered. |

### Group 10 — observation leftovers, explicitly not done

Not group 9 (census exclusion / gem name / fsync). These rows closed *and named what they did not do*. Do not run `graph.replay` on the live volume to close 68. Do not extract `ROLE=project-graph` to spend a thirteenth slot. Do not edit dated ADRs to make live prose match.

| Order | Item | Do | Why |
|---:|---|---|---|
| 32 | **68** | leave U1 / U4 / U8 observation-only; U2 stays nil | closed. Contract changes were U3/U5/U6/U7. Did not touch idempotency `get`. **Did not run `graph.replay` on the live volume.** |
| 33 | **63** | UNCLEAR stays out of the collector | closed as collector. "8 UNCLEAR remain (64, 65)" — 64/65/68 closed the contract ones. Observation-only is 68. Do not expand RefusalLog to mint the rest. |
| 34 | **79** | do not edit dated ADRs | closed for *live* prose. "ADRs deliberately untouched — a dated record gets an amendment, not an edit." Group 7 order 16 is the 0050 amendment. This is the rest of the corpus. |
| 35 | **7** | project-graph stays embedded in BACK | closed. "12th container spent on nats." Async `ROLE=project-graph` is no longer the slot. ROW7 option 1: measured working. |

### Group 11 — undeclared seam, recorded shape deltas

Not group 8 (110 unowned NodeShapes in the live census). Not group 4 (switch Node leftover). A live seam with no scope, and two measurements that must not be "fixed" by weakening a gate.

| Order | Item | Do | Why |
|---:|---|---|---|
| 36 | `switchyard-offline` | contract decision on scope, not a guess | `.cpcp/package.json` `unscoped_seams`: live `POST /_cpcp/rpc`, browser-local credential routing, **UNDECLARED**. None of the three scopes is obviously right. Not a pod-network seam. |
| 37 | **67** | leave the packaged-vs-legacy delta recorded | closed. Packaged P9 is strictly stricter (`rdf:type` alone under `sh:closed`). A comment-insensitive hasher would bury it. Gate not weakened. |
| 38 | **103** | leave the 9 conflicts that do not touch a bound_runtime shape | closed by running it properly. Constraint-ledger: 9 conflict, **0 touching a bound_runtime shape**. Do not "fix" unbound conflicts by shrinking the census. |

### Group 12 — superseded closed-row measurements

Not group 10 (named not-done that remain true). Not group 9 (the count is 14). Closed rows whose cells still describe a prior world. Do not reopen. The later row or live table is the authority. Do not register dated documents to make a live gate happy.

| Order | Item | Do | Why |
|---:|---|---|---|
| 39 | **20** | leave the closed measurement; `seam_authority.json` is live | closed: live **2** (BACK, switchyard-offline local); vault decided-unbuilt. Live table now: back, switchyard-offline, vault, bus, persist, mind, rag. `decided_unbuilt: []`. |
| 40 | **50** | do not reopen as REST stand-in | closed: vault decided-unbuilt, REST stand-in, "Did not build config-admin". Row 4: REST retired, CPCP serving. Row 5: config-admin closed. TTL not authored stays group 5. |
| 41 | **8** | row 48 is closed as served | Gap cell still says "row 48 still decided-unbuilt". 48 closed as served. Persist closed. |
| 42 | SWITCHYARD.md / pod-to-vps.md | stay unregistered | row 121 remainder. Their 12 is a dated decision premise and a verbatim quotation. A gate enforcing what the pod IS must not rewrite what a dated decision SAID. Group 9 was stop picking 12 or 14. |

### Group 13 — unenforced flags whose because names a landing as unbuilt

Not group 7 (amend 0050's decision text). Not group 10 (do not edit dated ADRs). Not group 12 (superseded coverage-row cells). These ADRs still say `unenforced: true` because a ROLE was unbuilt. The ROLE landed. Do not drop the flag from this table (gap 97). Do not rewrite because from this table (group 10).

| Order | Item | Do | Why |
|---:|---|---|---|
| 43 | **0048** | leave unenforced; CI wiring stays group 1 | `unenforced_because`: MIND `/_cpcp` unbuilt (row 10). `seam_authority.json` lists mind live. Dropping the flag with no enforcing target is gap 97. |
| 44 | **0055** | leave unenforced; bus landed | `unenforced_because`: ROLE=bus unbuilt (row 9). Row 9 RUNS. `enforced_by: []`. Group 7 is the 0050 amendment and the first versioned consumer. |
| 45 | **9** | leave the unbuilt scheduled caller of `bus.projection.latest` | GAP18 recorded it rather than inventing one. 0055's because names the *container* unbuilt; the container runs. The leftover is the caller. |

### Group 14 — ungated human pin, and facts the pin landing recorded

Not group 2 (the compose swap onto `front-base`). Not group 12 (superseded coverage-row cells). `32b1d6d` rebuilt and repinned `FLOOR-FRONT.json`. A green sweep still does not validate that pin. Do not invent a checker from this table. Do not auto-bump. Do not swap compose from this table.

| Order | Item | Do | Why |
|---:|---|---|---|
| 46 | `FLOOR-FRONT.json` | leave ungated | `32b1d6d`: nothing under `tooling/` reads it. A green sweep does not validate this pin. Group 2 is the human compose swap. |
| 47 | overlay `FROM front-base:local` | leave the tag form | measured: `FROM front-base@sha256:<id>` exit 1 for a never-pushed local image; tag form exit 0. Digest-pinned ARG would not have built here. |
| 48 | `published_images.json` overlay because | the "until re-measured" clause is now dated | `not_published` mind-pod FRONT still says the base pin is `front-base:local` until `FLOOR-FRONT.json` is re-measured by hand. `32b1d6d` re-measured. Do not auto-bump. |

### Group 15 — not this tree, or filling it here would lie

Not group 8 (3-class publication / W3ID). Not group 14 (ungated pin *in this tree*). Not group 2 (compose swap). Work that lives in another repo, or that would fabricate a property if closed from this table. Do not invent a row.

| Order | Item | Do | Why |
|---:|---|---|---|
| 49 | **0063** | leave the consumer pin ungated here | `unenforced_because`: the application pins the base digest and resolves substrate gems in the *application* repo. P4 landed `FRONT_OVERRIDE` in this substrate. The consumer half is not a coverage row here. |
| 50 | profiles `[]` | do not map methods to OSI profiles from this table | `boundary_manifest.json`: "no method is mapped to OSI profiles yet, and mapping is future work, not a guess." Group 8 is the 3-class + W3ID pledge. This is profile membership. |
| 51 | **60** | do not add `db:prepare` to FRONT | closed. FRONT boots `/up` and `/` with no sqlite in the image. Adding prepare would fake a FRONT database. |

### Group 16 — facts the FRONT compose swap recorded

Not group 2 (the compose swap — `046000c` landed it). Not group 14 (ungated pin). Both compose files now BUILD `runtimes/mind-pod/front` FROM `front-base:local` as `mind-pod-front`. The swap is not leftover. These are.

| Order | Item | Do | Why |
|---:|---|---|---|
| 52 | FRONT → BACK in-pod | leave unproven | `046000c`: pod not brought up end to end. Both compose files parse at 14 services and the images build. Nothing proves the wired FRONT reaches a live BACK on the pod network. |
| 53 | front `MM_NATS_URL` | leave declared, unread | `check_nats.CLIENTS` lists front. Dropping it fails the gate. Bun FRONT speaks CPCP HTTP to BACK at `BACK_CPCP_ORIGIN`. |
| 54 | `HTTP_BIND` default `0.0.0.0` | do not flip to `127.0.0.1` | would strand existing front-base containers, sharedai.space included. In-pod compose sets `127.0.0.1` and Bun.serve now honours it. |
| 55 | README "not yet swapped" | leave, or it is now dated | `language_rule.json` records the stopgap ended. Root README still says compose runs Rails FRONT (`mind-pod:demo`). Group 10 is dated ADRs. ContainerTopology was corrected in `046000c`. |

### Group 17 — the seventh image lineage, and facts the count fix recorded

Not group 9 (stop picking 12 or 14 — the container count is settled, row 121 closed). Not group 12 (superseded closed-row cells). Not group 16 (the FRONT swap — `046000c` landed it, and moved the §3 heading to 7 but not the target). The swap added the lineage; these are the numbers it did not move, plus the gate that keeps them moved.

| Order | Item | Do | Why |
|---:|---|---|---|
| 56 | Target 6 → 7 | count lineages, not services | the extract compose carries 7 distinct `image:` strings: Rails, FRONT overlay, MIND, SwitchYard, oxigraph, nats, milvus. Eight Rails ROLEs share one image. `rag` on an older tag is drift, not an eighth lineage (§3 says so). |
| 57 | §3 "six units" → seven | the same drift, one paragraph down | stale since `952ae1d`, before the overlay existed. The three third-party lineages still patch independently. |
| 58 | image claims registered | a number nothing counts drifts | `count_pod_images` plus two claims; population 9 → 11. The container claims counted the same two headings' first half and never saw the image half. |
| 59 | twelve / "unreconciled" prose → settled-at-14 | row 121 is closed | "Twelve containers exist today" and the target paragraph predate the reconciliation. §1 stays as the twelve-container diagram from before `rag` and `milvus`; §2 is the current measurement. |

### Group 18 — the double-landing, and facts the merge recorded

Not group 16 (the FRONT swap). Not group 17 (the fix — target 7, image claims, settled prose). The fix is not leftover. What needed recording is that it landed twice: main `f68dc50` and NextGaps17 `d97f8c8`, independently, and this merge is where they meet. No new code beyond conflict resolution; both sides green on the same 11 claims.

| Order | Item | Do | Why |
|---:|---|---|---|
| 60 | `f68dc50` is the landed fix | adopt as base, do not re-litigate | target 7, `count_pod_images` plus two claims, settled-at-14 paragraph with the "Both headings are registered claims" lesson. That lesson is the point — keep it verbatim. |
| 61 | three spots the landing didn't move | kept from NextGaps17 | Read-this-first fourteen, §1 dated retitle, §3 "six units" → seven. Measured, same drift, same gate. Not a rival fix. |
| 62 | counter docstring names the rag-tag edge | the one semantic addition over the landing | distinct `image:` strings coincide with lineages only because the extract compose carries one tag per lineage; the dev compose splits `mind-pod:latest` / `:demo`. The counter's correctness depends on that sentence. |

### Group 19 — prose the swap dated

Not group 10 (dated ADRs — this is the live README and the live ledger). Not group 14 (the pin landing — order 48 recorded the dating; this fixes the sentence). Not group 16 (the swap — order 55 recorded the dating; this fixes the paragraph). The swap landed; these two sentences still describe the world before it.

| Order | Item | Do | Why |
|---:|---|---|---|
| 63 | README "not yet swapped" → swapped-but-unproven | correct the mechanism, keep the caution | compose builds `front` FROM `front-base:local` — "still runs the Rails FRONT until a human act" is false, and the paragraph above it already describes the swapped world. What stays ahead of deployment is narrower: nothing proves the wired FRONT reaches a live BACK (order 52). |
| 64 | ledger FRONT because: lineage + re-measured clause | both halves false-or-dated | "adds no image lineage" is false since group 17 (the overlay ships as `mind-pod-front`, the seventh lineage); "until re-measured by hand" is dated since `32b1d6d` (order 48). The entry stays `not_published` — the overlay builds FROM the published base rather than publishing itself. |
| 65 | `check_published_images` holds structure, not truth | record the gap, do not extend the gate from this table | a false `because` passes: non-empty is all it asks. Truth-gating prose is a new checker with its own plants, not a docs-table edit (gap 97: enforcement claims from a table fake enforcement). |
