# Rows 86/87 — LOG: stage map against what exists

Measured 2026-09-03 from `ab047b8`. **Design only. Not built.** Rows stay
decided-unbuilt; this scopes the stages so the sink decision can land.

Manus 2026-08-31d as decided: LOG is a governed aggregation path (OTLP
volume transport, CPCP contract), never the only record; local append is
the record of first resort; the outside observer stays explicitly
UNNAMED (row 87: leaving it unbuilt is more honest than letting LOG
appear to close 0054).

## Stage map

| Stage | Decided | Exists today |
|---|---|---|
| 1. Local append-only floor in every producer | yes | **yes, measured**: `RefusalLog` JSONL + heartbeat (gap 63 collector), floor behavior under restart/chmod/first-write/SIGKILL/disk in [`GAP89_FLOOR.md`](../archive/findings/GAP89_FLOOR.md), self-loss reasoning in [`GAP89_SELF.md`](../archive/findings/GAP89_SELF.md) |
| 2. Bounded file batching (rotation + loud markers) | yes, file terms | **built**: explicit `rotate!` (generations capped, `floor_rotated` marker names the drop), `status` reader; write path untouched so gap-89 evidence stands |
| 3. CPCP control-plane surface for LOG | deferred | **no**: no LOG methods; `status` is local inspection, not a seam |
| 4. Aggregation + durable handoff | yes | **convention only**: per-role JSONL at `<role>/log/`, collected via existing volume backup discipline; no shipper |
| 5. Outside observer (WATCH + RETAIN outside the refuser's failure domain) | **explicitly unbuilt** (row 87) | no, by decision |

## Next (owner-gated)

* Sink decided 2026-09-03: **file handoff only** (no collector, no
  exporter). Stages 2 (rotation/markers) and 4 (handoff convention)
  built on those terms.
* The observer stays unnamed until a failure domain outside the pod
  exists to host it. CI is provably not it.

## What this is not

* Not a build. Not a container. Not an OTLP dependency.
* Not a reopening of 0054 (LOG must not claim to close it).
* Not per-record CPCP admission for log volume (rejected by decision).
