# Gap 107: ContextFrame / Meaning / Clarification are shapes

The sole inter-application message is modelled as SHACL. Not a CPCP
operation yet (no wrap). Not published as a w3id.org redirect.

## Containment, not three peers

Board columns are Input / Frame / Translation. Meaning and
Clarification sit **under** Frame. A clarification sits under a
meaning. Input is frame-independent (not in these shapes).

| shape | must |
|---|---|
| `cf:ContextFrameShape` | `sh:targetClass cf:ContextFrame`; may have `cf:meaning` |
| `cf:MeaningShape` | `cf:inContextFrame` minCount 1 maxCount 1 |
| `cf:ClarificationShape` | `cf:inMeaning` minCount 1 maxCount 1 |

Namespace: `https://w3id.org/cpcp/osi8/contextframe#` (ADR 0060
convention, new topic). One prefix holds shapes and properties.

## What these are not

- Not `p8:FrameChange` / `frameName` / `frameDigest` (how the team works).
- Not `ux:PanelFrame` (a UI kind).
- Not profile-11 (`Concept`, `DefinitionRevision`, attestation).
  `profile-11-meaning.ttl` was not edited.

File: `gems/shapes-level-8/bundles/contextframe.shacl.ttl` (protocol
packaging home: another app would reuse this message unchanged).

## Gate

`tooling/shacl/check_contextframe_containment.py`. Plants: empty
CHECK_ROOT; Meaning not contained (`minCount 0`); p8 FrameChange reuse.

Did not add catalog `P*::` keys (no profile number, no wrap). Did not
port catalog.mjs. Did not start the non-all-200 plan.
