# Architecture Decision Records

One file per decision, numbered and immutable once accepted. Anything that moves
a boundary, changes a contract, or advances an upstream pin requires an ADR.

Format: Context → Decision → Consequences. See [`0001-ownership-boundary.md`](0001-ownership-boundary.md).
