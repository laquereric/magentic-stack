# SBOM + provenance gate — STUB.
# Release integrity: every release records source revision, license, SBOM,
# provenance, conformance, and rollback targets for the upstream pins.
name: sbom

on:
  push:
    branches: [ main ]
  pull_request:
    paths:
      - 'upstreams/**'

jobs:
  sbom:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify upstream pin records are complete
        run: |
          echo "TODO: assert each upstreams/manifests/*.pin.json has"
          echo "      pinned_revision, license, sbom, provenance, rollback_target"
          echo "TODO: generate SBOM for pinned upstreams"
