# General CI — lints the scaffold + runs integration-tests/ (STUB).
# The release DECISION is made by release-gates.yml (the six pilot gates).
name: ci
on:
  push: { branches: [ main ] }
  pull_request:
jobs:
  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { submodules: recursive }
      - name: Integration tests
        run: |
          echo "TODO: run integration-tests/ (see docs/plans/pilot-release-gates.md)"
