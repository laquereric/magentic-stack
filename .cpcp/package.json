{
  "kind": "cpcp-application",
  "version": 1,
  "name": "magentic-stack",
  "description": "Runs the CPCP seams. One Rails image under many ROLEs, plus MIND (Python), SwitchYard (Rust upstream behind a Node front) and oxigraph.",
  "contract": {
    "repo": "https://github.com/laquereric/coordination-protocol-contract-package",
    "rev": "434ef847c883307caebaf0e18a1eb458a76c48b5",
    "note": "Scope vocabulary and the method-to-scope map come from spec/scopes.md at this revision. Adding a scope to a seam is a contract change there first. Referenced by repo and revision, not by path: ADR 0020 keeps gems/adapters/ the only code that reaches the vendored tree, and the pin records live in the manifests."
  },
  "registry": {
    "repo": "https://github.com/laquereric/cpcp_registry",
    "sha": "30975bad7f86a86ea5bd3e1ad74c53b7fcd00dac",
    "note": "Full SHA, never an abbreviation: an abbreviated rev is not a durable reference and stops being unique as the repo grows. Kept equal to the pin record for this submodule and to the gitlink, so the registry a reader resolves is the one the tree carries."
  },
  "scopes": {
    "source": "spec/scopes.md",
    "note": "One manifest per scope. A seam appears in every scope manifest it is reachable at -- the method contract is identical at every scope, exposure is not, so this is one seam under two exposure rules, never two seams.",
    "manifests": {
      "public_cpcp": ".cpcp/public_cpcp/package.json",
      "pod_internal_cpcp": ".cpcp/pod_internal_cpcp/package.json",
      "pod_external_cpcp": ".cpcp/pod_external_cpcp/package.json"
    }
  },
  "unscoped_seams": [
    {
      "id": "switchyard-offline",
      "scope": [],
      "routes": [
        "POST /_cpcp/rpc"
      ],
      "served_by": "gems/switchyard-offline/local-listener/server.mjs",
      "because": "UNDECLARED. This seam is live here but does not appear in the contract method-to-scope map. It is browser-local credential routing (switchyard.route, cpcp.ready, cid), not pod-network traffic, so none of the three scopes is obviously right. Needs a contract decision, not a guess here."
    }
  ],
  "not_a_seam": {
    "front": "route-gated off /_cpcp (ADR 0047 amendment 2). Publishes a page, admits nothing.",
    "runtimes/switch": "OpenAI-compatible completions, no /_cpcp. Node today, target Rust.",
    "shape": "GET-only retrieval, not a POST-rpc seam."
  },
  "gates": [
    "tooling/cpcp/check_seam_authority.py",
    "tooling/compose/check_role_routes.py",
    "tooling/compose/check_loopback_env.py",
    "tooling/boundary/check_boundary.py"
  ]
}
