<h1>Magentic Market Core</h1>
<p class="sub">FRONT is the host, not a notes app. Pair / bind, then enter an application.
Notes are nested. This is not the product homepage of Shared AI Space.</p>

<% if @error %><p class="err"><%= @error %></p><% end %>

<div class="card">
  <strong>Applications this Actor may enter</strong>
  <ul>
    <li><a href="/notes">mind-pod notes</a> — nested (was <code>/</code>; CANONICAL_GAPS P4)</li>
    <li>sharedai.space — canvas overlay on <code>front-base</code></li>
  </ul>
</div>

<div class="card">
  <strong>BACK</strong>
  <div class="sub"><code><%= @back %></code></div>
</div>
