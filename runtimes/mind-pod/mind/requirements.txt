# Declared MIND runtime pins. NOOA is installed from the vendored source
# (Dockerfile). pydantic is no longer a transitive accident: it shapes
# NoteInsight / BoardReading / SessionReading. Version is
# upstreams/manifests/pydantic.pin.json (kind: pypi). Lift with that pin.
pydantic==2.13.5
