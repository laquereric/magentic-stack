# NOOA is installed from the pinned vendored source (see Dockerfile), which brings
# pydantic + litellm. This file documents the direct runtime deps; the harness
# itself uses only the Python stdlib (urllib/json/hashlib) + NOOA.
