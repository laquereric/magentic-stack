{
  "name": "switch",
  "version": "0.1.0",
  "private": true,
  "description": "SwitchYard: the MIND pod's LLM plane. Local-first routing with a config UI.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20" },
  "license": "Apache-2.0"
}
