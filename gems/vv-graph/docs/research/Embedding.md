or a Mac with 24GB of RAM, the best setup is to use nomic-embed-text as your embedding model and sqlite-vec for storage. It is highly recommended to fetch the newer /api/embed endpoint (rather than the legacy /api/embeddings) to pass your text into SQLite.Why This Setup?Embedding Model: nomic-embed-text creates a 768-dimensional vector, requires very little memory (under 300MB), and runs effortlessly without maxing out your 24GB unified memory. It is highly capable for general-purpose text retrieval.Database & Search: sqlite-vec brings fast, local vector searches directly to the lightweight SQLite environment.Quick Setup & Workflow StepsPull the Model: Open your terminal and download the Nomic embedding model via Ollama:bashollama pull nomic-embed-text
Use code with caution.Install the Database: You can add the vector extension to your SQLite environment using the official sqlite-vec documentation.Generate & Store Embeddings: Pass your data to Ollama using the /api/embed endpoint to get the vectors, then insert them into a virtual table in your .db file.Example POST request URL: http://localhost:11434/api/embedSemantic Search: When a user asks a question, embed the query text through Ollama, and use sqlite-vec's distance math to retrieve the most semantically
