# PLAN 0.1.0 — vv-mobile skeleton

Emit a Foundation-only Swift package from a YAML/Hash spec. Package
compiles for iOS 17+ and the Swift 6.3 Android SDK triple
`aarch64-unknown-linux-android28`. No UI. Sisters `vv-ios` / `vv-android`
consume the same spec.

Status: landed (v0.1.0).
