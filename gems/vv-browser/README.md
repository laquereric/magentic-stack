# vv-browser

Moved from `magentic-market-ai/gems/mmg-browser`. `Vv::Browser` is the
module; `Mmg::Browser` is an alias.

Tests live here (`spec/`, `features/`). CPCP: `browser.inspect`,
`browser.evaluate`.
