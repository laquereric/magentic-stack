# vv-code-repo 🟡 CONTRACT ONLY

ProcedureRepo as a private gem. Digest is the name. Gold is what PROD
reads. Plan: [`docs/architecture/plan_procedure_repo.md`](../../docs/architecture/plan_procedure_repo.md).

**Built:** refusals, `procedure.*` operation list, land/bind/serve/promote
envelopes, DEV grant (`PROCEDURE_WRITE=1`).

**Not built:** ActiveRecord tables, blob IO, CPCP dispatcher, DuckDB.
