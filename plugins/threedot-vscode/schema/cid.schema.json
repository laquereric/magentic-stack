{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://threedot.dev/schema/cid.json",
  "title": "Cyborg Interface Descriptor",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "@context": { "type": "string", "description": "JSON-LD context IRI (live CID)." },
    "@id": { "type": "string", "description": "Stable IRI for this CID." },
    "title": { "type": "string" },
    "backUrl": {
      "type": "string",
      "description": "Discovery seed only: rails-threedot-back base URL. Live CID comes from GET <back>/_cpcp/cid.json."
    },
    "cpcpPath": {
      "type": "string",
      "description": "Discovery seed only: CPCP mount path (default /_cpcp)."
    },
    "operations": {
      "type": "array",
      "description": "Live CID operations. Optional on discovery seed files.",
      "items": { "$ref": "#/definitions/op" }
    }
  },
  "definitions": {
    "op": {
      "type": "object",
      "required": ["@id", "name", "role", "summary"],
      "additionalProperties": true,
      "properties": {
        "@id": { "type": "string" },
        "name": { "type": "string", "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" },
        "role": { "type": "string", "enum": ["context", "effect", "component", "token"] },
        "summary": { "type": "string" },
        "params": {
          "type": "object",
          "properties": {
            "required": { "type": "array", "items": { "type": "string" } },
            "closed": { "type": "boolean" },
            "properties": { "type": "object" }
          }
        },
        "result": {
          "type": "object",
          "properties": { "shape": { "type": "string" } }
        }
      }
    }
  }
}
