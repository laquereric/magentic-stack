# Open this file in the UI test. With a live BACK connected, typing `three.` yields
# CID-native completions (note.list / note.create / l8.* ...). See test/README.md.
import three  # noqa

# three.noteList(...)
# three.noteCreate({ "operationId": "ui-demo-1", "title": "...", "body": "..." })
