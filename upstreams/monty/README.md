# upstreams/monty/  🟡 FOLLOW THEM

Pinned pydantic/monty. OS-level isolation is MIND's container; monty is
the *interpreter* isolation seam for CodeAct (ADR 0070).

Reached only through [`../../gems/adapters/monty/`](../../gems/adapters/monty/).
Do not import this tree from `runtimes/`. The gitlink is `submodule_path`
in `upstreams/manifests/monty.pin.json`.
